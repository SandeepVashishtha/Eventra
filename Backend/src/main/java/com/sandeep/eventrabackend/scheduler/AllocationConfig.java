package com.sandeep.eventrabackend.scheduler;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AllocationConfig {
    // Configuration properties mapping waitlist queue expirations
}
