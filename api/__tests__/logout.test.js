describe('logout handler', () => {
  it('should accept token from HttpOnly cookie when Authorization header is absent', () => {
    // Test stub: logout now reads req.cookies.token and raw cookie header
  });
});
