# Mobile Header Button Alignment Guidelines

Ensure header navigation buttons are cleanly stacked on narrow screens.

## CSS Rules
```css
.header-actions {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  gap: 8px;
}
```