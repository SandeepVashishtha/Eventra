# test/e2e-auth — Plan

Goal: Add Playwright E2E tests covering login, signup, OAuth redirect handling and session recovery.

Work items:
- Add `tests/e2e/login.spec.js` and `signup.spec.js` with real flows or mocked providers
- Add offline/session recovery scenarios
- Integrate with CI to run e2e on pull requests

This branch contains scaffolding and initial placeholders.
