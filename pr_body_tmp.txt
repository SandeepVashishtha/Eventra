This PR cleans up duplicate cryptographic identifier definitions within `src/utils/secureStorage.js` that caused fatal syntax errors at module import. No features are removed; the two overlapping implementations are consolidated.

Fixes #3669