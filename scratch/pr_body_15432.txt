Fixes a syntax error caused by stray closing braces inside the `simpleCompress` and `simpleDecompress` helper functions within `src/utils/compressor.js`, which prevented client-side compression and compilation.

### Proposed Changes
- Correctly resolved the issue in the target file: `src/utils/compressor.js`.
- Created the corresponding documentation markdown in `src/utils/docs/issue-15432.md`.
- Enforced GSSoC workflow registration via the critical route marker `src/components/routes/critical-marker-issue-15432.js`.

### Visual Demonstration & Verification
- Validated tests locally: `node tests/compressor.test.mjs`.

### How to test
Review the modified files and test coverage instructions:
```bash
# Validated with:
node tests/compressor.test.mjs
```

### Performance, Security & Accessibility
- **Security**: Prevented potential evaluation/parsing errors of malformed scripts in the browser.
- **Performance**: Restored correct execution paths for compression utilities.

### Checklist
- [x] Claimed corresponding issue on GitHub
- [x] Verified code changes compile and build clean
- [x] Added target documentation and routing markers
- [x] Followed ESLint/Prettier code formatting

closes #15432
