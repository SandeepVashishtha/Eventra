import { describe, it, expect } from 'vitest';
import * as AverageStack from '../average-stack.js';

describe('average-stack', () => {
  it('exports a module', () => {
    expect(AverageStack).toBeDefined();
  });
});

