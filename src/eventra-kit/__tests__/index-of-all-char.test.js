import { describe, it, expect } from 'vitest';
import * as IndexOfAllChar from '../index-of-all-char.js';

describe('index-of-all-char', () => {
  it('exports a module', () => {
    expect(IndexOfAllChar).toBeDefined();
  });
});

