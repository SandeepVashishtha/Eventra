import { describe, it, expect } from 'vitest';
import * as DedupePair from '../dedupe-pair.js';

describe('dedupe-pair', () => {
  it('exports a module', () => {
    expect(DedupePair).toBeDefined();
  });
});

