import { describe, it, expect } from 'vitest';
import * as RemoveRangeText from '../remove-range-text.js';

describe('remove-range-text', () => {
  it('exports a module', () => {
    expect(RemoveRangeText).toBeDefined();
  });
});

