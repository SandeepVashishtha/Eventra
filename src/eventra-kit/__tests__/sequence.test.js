import { describe, it, expect } from 'vitest';
import * as Sequence from '../sequence.js';

describe('sequence', () => {
  it('exports a module', () => {
    expect(Sequence).toBeDefined();
  });
});

