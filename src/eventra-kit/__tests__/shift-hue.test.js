import { describe, it, expect } from 'vitest';
import * as ShiftHue from '../shift-hue.js';

describe('shift-hue', () => {
  it('exports a module', () => {
    expect(ShiftHue).toBeDefined();
  });
});

