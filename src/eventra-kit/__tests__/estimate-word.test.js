import { describe, it, expect } from 'vitest';
import * as EstimateWord from '../estimate-word.js';

describe('estimate-word', () => {
  it('exports a module', () => {
    expect(EstimateWord).toBeDefined();
  });
});

