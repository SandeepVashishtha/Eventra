import { describe, it, expect } from 'vitest';
import * as CheckDate from '../check-date.js';

describe('check-date', () => {
  it('exports a module', () => {
    expect(CheckDate).toBeDefined();
  });
});

