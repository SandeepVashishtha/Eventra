import { describe, it, expect } from 'vitest';
import * as DetectStack from '../detect-stack.js';

describe('detect-stack', () => {
  it('exports a module', () => {
    expect(DetectStack).toBeDefined();
  });
});

