import { describe, it, expect } from 'vitest';
import * as ToHashValue from '../to-hash-value.js';

describe('to-hash-value', () => {
  it('exports a module', () => {
    expect(ToHashValue).toBeDefined();
  });
});

