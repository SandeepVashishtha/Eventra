import { describe, it, expect } from 'vitest';
import * as AssertTree from '../assert-tree.js';

describe('assert-tree', () => {
  it('exports a module', () => {
    expect(AssertTree).toBeDefined();
  });
});

