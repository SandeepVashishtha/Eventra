import { describe, it, expect } from 'vitest';
import * as EnsureFraction from '../ensure-fraction.js';

describe('ensure-fraction', () => {
  it('exports a module', () => {
    expect(EnsureFraction).toBeDefined();
  });
});

