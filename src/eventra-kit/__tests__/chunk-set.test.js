import { describe, it, expect } from 'vitest';
import * as ChunkSet from '../chunk-set.js';

describe('chunk-set', () => {
  it('exports a module', () => {
    expect(ChunkSet).toBeDefined();
  });
});

