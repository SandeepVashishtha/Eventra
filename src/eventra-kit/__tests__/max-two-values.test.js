import { describe, it, expect } from 'vitest';
import * as MaxTwoValues from '../max-two-values.js';

describe('max-two-values', () => {
  it('exports a module', () => {
    expect(MaxTwoValues).toBeDefined();
  });
});

