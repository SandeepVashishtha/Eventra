import { describe, it, expect } from 'vitest';
import * as DetectStep from '../detect-step.js';

describe('detect-step', () => {
  it('exports a module', () => {
    expect(DetectStep).toBeDefined();
  });
});

