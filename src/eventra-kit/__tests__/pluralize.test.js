import { describe, it, expect } from 'vitest';
import * as Pluralize from '../pluralize.js';

describe('pluralize', () => {
  it('exports a module', () => {
    expect(Pluralize).toBeDefined();
  });
});

