import { describe, it, expect } from 'vitest';
import * as ConvertFraction from '../convert-fraction.js';

describe('convert-fraction', () => {
  it('exports a module', () => {
    expect(ConvertFraction).toBeDefined();
  });
});

