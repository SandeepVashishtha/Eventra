import { describe, it, expect } from 'vitest';
import * as IsString from '../is-string.js';

describe('is-string', () => {
  it('exports a module', () => {
    expect(IsString).toBeDefined();
  });
});

