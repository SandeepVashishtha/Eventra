import { describe, it, expect } from 'vitest';
import * as ExtractNumbers from '../extract-numbers.js';

describe('extract-numbers', () => {
  it('exports a module', () => {
    expect(ExtractNumbers).toBeDefined();
  });
});

