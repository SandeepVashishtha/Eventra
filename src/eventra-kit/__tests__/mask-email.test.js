import { describe, it, expect } from 'vitest';
import * as MaskEmail from '../mask-email.js';

describe('mask-email', () => {
  it('exports a module', () => {
    expect(MaskEmail).toBeDefined();
  });
});

