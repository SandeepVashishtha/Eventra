import { describe, it, expect } from 'vitest';
import * as IsOlderThan from '../is-older-than.js';

describe('is-older-than', () => {
  it('exports a module', () => {
    expect(IsOlderThan).toBeDefined();
  });
});

