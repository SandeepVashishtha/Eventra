import { describe, it, expect } from 'vitest';
import * as InsertRange from '../insert-range.js';

describe('insert-range', () => {
  it('exports a module', () => {
    expect(InsertRange).toBeDefined();
  });
});

