import { describe, it, expect } from 'vitest';
import * as GetValueByPath from '../get-value-by-path.js';

describe('get-value-by-path', () => {
  it('exports a module', () => {
    expect(GetValueByPath).toBeDefined();
  });
});

