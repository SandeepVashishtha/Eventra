import { describe, it, expect } from 'vitest';
import * as ToArrayIfNeeded from '../to-array-if-needed.js';

describe('to-array-if-needed', () => {
  it('exports a module', () => {
    expect(ToArrayIfNeeded).toBeDefined();
  });
});

