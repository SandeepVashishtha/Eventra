import { describe, it, expect } from 'vitest';
import * as ToLocalTime from '../to-local-time.js';

describe('to-local-time', () => {
  it('exports a module', () => {
    expect(ToLocalTime).toBeDefined();
  });
});

