import { describe, it, expect } from 'vitest';
import * as ToFixedPercent from '../to-fixed-percent.js';

describe('to-fixed-percent', () => {
  it('exports a module', () => {
    expect(ToFixedPercent).toBeDefined();
  });
});

