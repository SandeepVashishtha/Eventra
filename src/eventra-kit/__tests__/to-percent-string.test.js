import { describe, it, expect } from 'vitest';
import * as ToPercentString from '../to-percent-string.js';

describe('to-percent-string', () => {
  it('exports a module', () => {
    expect(ToPercentString).toBeDefined();
  });
});

