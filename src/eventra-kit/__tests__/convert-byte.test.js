import { describe, it, expect } from 'vitest';
import * as ConvertByte from '../convert-byte.js';

describe('convert-byte', () => {
  it('exports a module', () => {
    expect(ConvertByte).toBeDefined();
  });
});

