import { describe, it, expect } from 'vitest';
import * as CheckChunk from '../check-chunk.js';

describe('check-chunk', () => {
  it('exports a module', () => {
    expect(CheckChunk).toBeDefined();
  });
});

