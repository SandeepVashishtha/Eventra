import { describe, it, expect } from 'vitest';
import * as ComputeXml from '../compute-xml.js';

describe('compute-xml', () => {
  it('exports a module', () => {
    expect(ComputeXml).toBeDefined();
  });
});

