import { describe, it, expect } from 'vitest';
import * as DiffPath from '../diff-path.js';

describe('diff-path', () => {
  it('exports a module', () => {
    expect(DiffPath).toBeDefined();
  });
});

