import { describe, it, expect } from 'vitest';
import * as IsPositive from '../is-positive.js';

describe('is-positive', () => {
  it('exports a module', () => {
    expect(IsPositive).toBeDefined();
  });
});

