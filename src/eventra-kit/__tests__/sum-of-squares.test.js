import { describe, it, expect } from 'vitest';
import * as SumOfSquares from '../sum-of-squares.js';

describe('sum-of-squares', () => {
  it('exports a module', () => {
    expect(SumOfSquares).toBeDefined();
  });
});

