import { describe, it, expect } from 'vitest';
import * as DetectHtml from '../detect-html.js';

describe('detect-html', () => {
  it('exports a module', () => {
    expect(DetectHtml).toBeDefined();
  });
});

