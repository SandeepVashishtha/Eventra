import { describe, it, expect } from 'vitest';
import * as RoundToNearest from '../round-to-nearest.js';

describe('round-to-nearest', () => {
  it('exports a module', () => {
    expect(RoundToNearest).toBeDefined();
  });
});

