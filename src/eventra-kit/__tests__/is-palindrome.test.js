import { describe, it, expect } from 'vitest';
import * as IsPalindrome from '../is-palindrome.js';

describe('is-palindrome', () => {
  it('exports a module', () => {
    expect(IsPalindrome).toBeDefined();
  });
});

