import { describe, it, expect } from 'vitest';
import * as CountString from '../count-string.js';

describe('count-string', () => {
  it('exports a module', () => {
    expect(CountString).toBeDefined();
  });
});

