import { describe, it, expect } from 'vitest';
import * as CheckBlock from '../check-block.js';

describe('check-block', () => {
  it('exports a module', () => {
    expect(CheckBlock).toBeDefined();
  });
});

