import { describe, it, expect } from 'vitest';
import * as CountOverlap from '../count-overlap.js';

describe('count-overlap', () => {
  it('exports a module', () => {
    expect(CountOverlap).toBeDefined();
  });
});

