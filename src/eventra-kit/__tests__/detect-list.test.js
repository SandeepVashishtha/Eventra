import { describe, it, expect } from 'vitest';
import * as DetectList from '../detect-list.js';

describe('detect-list', () => {
  it('exports a module', () => {
    expect(DetectList).toBeDefined();
  });
});

