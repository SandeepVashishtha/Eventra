import { describe, it, expect } from 'vitest';
import * as UnionArrays from '../union-arrays.js';

describe('union-arrays', () => {
  it('exports a module', () => {
    expect(UnionArrays).toBeDefined();
  });
});

