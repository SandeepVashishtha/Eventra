import { describe, it, expect } from 'vitest';
import * as DiffSegment from '../diff-segment.js';

describe('diff-segment', () => {
  it('exports a module', () => {
    expect(DiffSegment).toBeDefined();
  });
});

