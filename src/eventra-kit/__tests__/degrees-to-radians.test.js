import { describe, it, expect } from 'vitest';
import * as DegreesToRadians from '../degrees-to-radians.js';

describe('degrees-to-radians', () => {
  it('exports a module', () => {
    expect(DegreesToRadians).toBeDefined();
  });
});

