import { describe, it, expect } from 'vitest';
import * as DetectLine from '../detect-line.js';

describe('detect-line', () => {
  it('exports a module', () => {
    expect(DetectLine).toBeDefined();
  });
});

