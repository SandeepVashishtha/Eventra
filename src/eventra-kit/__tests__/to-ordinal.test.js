import { describe, it, expect } from 'vitest';
import * as ToOrdinal from '../to-ordinal.js';

describe('to-ordinal', () => {
  it('exports a module', () => {
    expect(ToOrdinal).toBeDefined();
  });
});

