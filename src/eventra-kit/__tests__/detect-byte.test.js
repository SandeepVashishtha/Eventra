import { describe, it, expect } from 'vitest';
import * as DetectByte from '../detect-byte.js';

describe('detect-byte', () => {
  it('exports a module', () => {
    expect(DetectByte).toBeDefined();
  });
});

