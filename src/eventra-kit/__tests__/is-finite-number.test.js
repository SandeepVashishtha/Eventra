import { describe, it, expect } from 'vitest';
import * as IsFiniteNumber from '../is-finite-number.js';

describe('is-finite-number', () => {
  it('exports a module', () => {
    expect(IsFiniteNumber).toBeDefined();
  });
});

