import { describe, it, expect } from 'vitest';
import * as DetectDict from '../detect-dict.js';

describe('detect-dict', () => {
  it('exports a module', () => {
    expect(DetectDict).toBeDefined();
  });
});

