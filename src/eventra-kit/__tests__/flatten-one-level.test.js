import { describe, it, expect } from 'vitest';
import * as FlattenOneLevel from '../flatten-one-level.js';

describe('flatten-one-level', () => {
  it('exports a module', () => {
    expect(FlattenOneLevel).toBeDefined();
  });
});

