import { describe, it, expect } from 'vitest';
import * as StringContains from '../string-contains.js';

describe('string-contains', () => {
  it('exports a module', () => {
    expect(StringContains).toBeDefined();
  });
});

