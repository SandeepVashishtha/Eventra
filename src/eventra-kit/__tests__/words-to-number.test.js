import { describe, it, expect } from 'vitest';
import * as WordsToNumber from '../words-to-number.js';

describe('words-to-number', () => {
  it('exports a module', () => {
    expect(WordsToNumber).toBeDefined();
  });
});

