import { describe, it, expect } from 'vitest';
import * as CollectBy from '../collect-by.js';

describe('collect-by', () => {
  it('exports a module', () => {
    expect(CollectBy).toBeDefined();
  });
});

