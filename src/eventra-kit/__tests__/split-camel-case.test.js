import { describe, it, expect } from 'vitest';
import * as SplitCamelCase from '../split-camel-case.js';

describe('split-camel-case', () => {
  it('exports a module', () => {
    expect(SplitCamelCase).toBeDefined();
  });
});

