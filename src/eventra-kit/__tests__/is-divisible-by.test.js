import { describe, it, expect } from 'vitest';
import * as IsDivisibleBy from '../is-divisible-by.js';

describe('is-divisible-by', () => {
  it('exports a module', () => {
    expect(IsDivisibleBy).toBeDefined();
  });
});

