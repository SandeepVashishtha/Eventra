import { describe, it, expect } from 'vitest';
import * as ToPlural from '../to-plural.js';

describe('to-plural', () => {
  it('exports a module', () => {
    expect(ToPlural).toBeDefined();
  });
});

