import { describe, it, expect } from 'vitest';
import * as DifferenceInHours from '../difference-in-hours.js';

describe('difference-in-hours', () => {
  it('exports a module', () => {
    expect(DifferenceInHours).toBeDefined();
  });
});

