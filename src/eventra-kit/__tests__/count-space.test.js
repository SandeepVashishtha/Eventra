import { describe, it, expect } from 'vitest';
import * as CountSpace from '../count-space.js';

describe('count-space', () => {
  it('exports a module', () => {
    expect(CountSpace).toBeDefined();
  });
});

