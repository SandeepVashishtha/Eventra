import { describe, it, expect } from 'vitest';
import * as IsSorted from '../is-sorted.js';

describe('is-sorted', () => {
  it('exports a module', () => {
    expect(IsSorted).toBeDefined();
  });
});

