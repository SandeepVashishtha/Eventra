import { describe, it, expect } from 'vitest';
import * as ParseQueryString from '../parse-query-string.js';

describe('parse-query-string', () => {
  it('exports a module', () => {
    expect(ParseQueryString).toBeDefined();
  });
});

