import { describe, it, expect } from 'vitest';
import * as CreateArray from '../create-array.js';

describe('create-array', () => {
  it('exports a module', () => {
    expect(CreateArray).toBeDefined();
  });
});

