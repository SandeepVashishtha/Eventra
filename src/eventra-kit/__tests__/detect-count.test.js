import { describe, it, expect } from 'vitest';
import * as DetectCount from '../detect-count.js';

describe('detect-count', () => {
  it('exports a module', () => {
    expect(DetectCount).toBeDefined();
  });
});

