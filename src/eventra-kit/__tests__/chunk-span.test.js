import { describe, it, expect } from 'vitest';
import * as ChunkSpan from '../chunk-span.js';

describe('chunk-span', () => {
  it('exports a module', () => {
    expect(ChunkSpan).toBeDefined();
  });
});

