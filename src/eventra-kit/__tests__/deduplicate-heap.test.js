import { describe, it, expect } from 'vitest';
import * as DeduplicateHeap from '../deduplicate-heap.js';

describe('deduplicate-heap', () => {
  it('exports a module', () => {
    expect(DeduplicateHeap).toBeDefined();
  });
});

