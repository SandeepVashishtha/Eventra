import { describe, it, expect } from 'vitest';
import * as ChunkHtml from '../chunk-html.js';

describe('chunk-html', () => {
  it('exports a module', () => {
    expect(ChunkHtml).toBeDefined();
  });
});

