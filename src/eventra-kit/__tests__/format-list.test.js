import { describe, it, expect } from 'vitest';
import * as FormatList from '../format-list.js';

describe('format-list', () => {
  it('exports a module', () => {
    expect(FormatList).toBeDefined();
  });
});

