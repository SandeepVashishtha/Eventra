import { describe, it, expect } from 'vitest';
import * as DiffChunk from '../diff-chunk.js';

describe('diff-chunk', () => {
  it('exports a module', () => {
    expect(DiffChunk).toBeDefined();
  });
});

