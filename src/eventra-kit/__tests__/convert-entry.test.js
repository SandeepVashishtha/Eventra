import { describe, it, expect } from 'vitest';
import * as ConvertEntry from '../convert-entry.js';

describe('convert-entry', () => {
  it('exports a module', () => {
    expect(ConvertEntry).toBeDefined();
  });
});

