import { describe, it, expect } from 'vitest';
import * as GetPercentage from '../get-percentage.js';

describe('get-percentage', () => {
  it('exports a module', () => {
    expect(GetPercentage).toBeDefined();
  });
});

