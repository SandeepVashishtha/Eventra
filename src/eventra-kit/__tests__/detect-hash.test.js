import { describe, it, expect } from 'vitest';
import * as DetectHash from '../detect-hash.js';

describe('detect-hash', () => {
  it('exports a module', () => {
    expect(DetectHash).toBeDefined();
  });
});

