import { describe, it, expect } from 'vitest';
import * as DetectFraction from '../detect-fraction.js';

describe('detect-fraction', () => {
  it('exports a module', () => {
    expect(DetectFraction).toBeDefined();
  });
});

