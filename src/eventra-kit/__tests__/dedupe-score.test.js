import { describe, it, expect } from 'vitest';
import * as DedupeScore from '../dedupe-score.js';

describe('dedupe-score', () => {
  it('exports a module', () => {
    expect(DedupeScore).toBeDefined();
  });
});

