import { describe, it, expect } from 'vitest';
import * as MarkdownBold from '../markdown-bold.js';

describe('markdown-bold', () => {
  it('exports a module', () => {
    expect(MarkdownBold).toBeDefined();
  });
});

