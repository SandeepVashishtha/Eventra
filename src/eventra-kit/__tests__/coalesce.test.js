import { describe, it, expect } from 'vitest';
import * as Coalesce from '../coalesce.js';

describe('coalesce', () => {
  it('exports a module', () => {
    expect(Coalesce).toBeDefined();
  });
});

