import { describe, it, expect } from 'vitest';
import * as ComputeBlock from '../compute-block.js';

describe('compute-block', () => {
  it('exports a module', () => {
    expect(ComputeBlock).toBeDefined();
  });
});

