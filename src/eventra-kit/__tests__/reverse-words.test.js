import { describe, it, expect } from 'vitest';
import * as ReverseWords from '../reverse-words.js';

describe('reverse-words', () => {
  it('exports a module', () => {
    expect(ReverseWords).toBeDefined();
  });
});

