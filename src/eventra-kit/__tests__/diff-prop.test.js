import { describe, it, expect } from 'vitest';
import * as DiffProp from '../diff-prop.js';

describe('diff-prop', () => {
  it('exports a module', () => {
    expect(DiffProp).toBeDefined();
  });
});

