import { describe, it, expect } from 'vitest';
import * as DateSort from '../date-sort.js';

describe('date-sort', () => {
  it('exports a module', () => {
    expect(DateSort).toBeDefined();
  });
});

