import { describe, it, expect } from 'vitest';
import * as IsBoolean from '../is-boolean.js';

describe('is-boolean', () => {
  it('exports a module', () => {
    expect(IsBoolean).toBeDefined();
  });
});

