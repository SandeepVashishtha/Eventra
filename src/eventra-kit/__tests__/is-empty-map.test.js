import { describe, it, expect } from 'vitest';
import * as IsEmptyMap from '../is-empty-map.js';

describe('is-empty-map', () => {
  it('exports a module', () => {
    expect(IsEmptyMap).toBeDefined();
  });
});

