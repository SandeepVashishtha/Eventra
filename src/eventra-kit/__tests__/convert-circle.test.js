import { describe, it, expect } from 'vitest';
import * as ConvertCircle from '../convert-circle.js';

describe('convert-circle', () => {
  it('exports a module', () => {
    expect(ConvertCircle).toBeDefined();
  });
});

