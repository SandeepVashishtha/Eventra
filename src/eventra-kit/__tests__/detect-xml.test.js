import { describe, it, expect } from 'vitest';
import * as DetectXml from '../detect-xml.js';

describe('detect-xml', () => {
  it('exports a module', () => {
    expect(DetectXml).toBeDefined();
  });
});

