import { describe, it, expect } from 'vitest';
import * as DaysInMonth from '../days-in-month.js';

describe('days-in-month', () => {
  it('exports a module', () => {
    expect(DaysInMonth).toBeDefined();
  });
});

