import { describe, it, expect } from 'vitest';
import * as DiffPoint from '../diff-point.js';

describe('diff-point', () => {
  it('exports a module', () => {
    expect(DiffPoint).toBeDefined();
  });
});

