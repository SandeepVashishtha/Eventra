import { describe, it, expect } from 'vitest';
import * as StandardDeviation from '../standard-deviation.js';

describe('standard-deviation', () => {
  it('exports a module', () => {
    expect(StandardDeviation).toBeDefined();
  });
});

