import { describe, it, expect } from 'vitest';
import * as DeepFlatten from '../deep-flatten.js';

describe('deep-flatten', () => {
  it('exports a module', () => {
    expect(DeepFlatten).toBeDefined();
  });
});

