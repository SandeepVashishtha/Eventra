import { describe, it, expect } from 'vitest';
import * as IsHiddenFile from '../is-hidden-file.js';

describe('is-hidden-file', () => {
  it('exports a module', () => {
    expect(IsHiddenFile).toBeDefined();
  });
});

