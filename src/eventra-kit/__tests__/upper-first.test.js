import { describe, it, expect } from 'vitest';
import * as UpperFirst from '../upper-first.js';

describe('upper-first', () => {
  it('exports a module', () => {
    expect(UpperFirst).toBeDefined();
  });
});

