import { describe, it, expect } from 'vitest';
import * as ExtractNumber from '../extract-number.js';

describe('extract-number', () => {
  it('exports a module', () => {
    expect(ExtractNumber).toBeDefined();
  });
});

