import { describe, it, expect } from 'vitest';
import * as EnsureUri from '../ensure-uri.js';

describe('ensure-uri', () => {
  it('exports a module', () => {
    expect(EnsureUri).toBeDefined();
  });
});

