import { describe, it, expect } from 'vitest';
import * as ContainsAll from '../contains-all.js';

describe('contains-all', () => {
  it('exports a module', () => {
    expect(ContainsAll).toBeDefined();
  });
});

