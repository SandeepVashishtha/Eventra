import { describe, it, expect } from 'vitest';
import * as SplitArrayInHalf from '../split-array-in-half.js';

describe('split-array-in-half', () => {
  it('exports a module', () => {
    expect(SplitArrayInHalf).toBeDefined();
  });
});

