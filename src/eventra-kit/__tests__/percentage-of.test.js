import { describe, it, expect } from 'vitest';
import * as PercentageOf from '../percentage-of.js';

describe('percentage-of', () => {
  it('exports a module', () => {
    expect(PercentageOf).toBeDefined();
  });
});

