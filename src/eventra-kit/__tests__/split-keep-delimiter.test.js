import { describe, it, expect } from 'vitest';
import * as SplitKeepDelimiter from '../split-keep-delimiter.js';

describe('split-keep-delimiter', () => {
  it('exports a module', () => {
    expect(SplitKeepDelimiter).toBeDefined();
  });
});

