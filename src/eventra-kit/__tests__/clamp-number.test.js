import { describe, it, expect } from 'vitest';
import * as ClampNumber from '../clamp-number.js';

describe('clamp-number', () => {
  it('exports a module', () => {
    expect(ClampNumber).toBeDefined();
  });
});

