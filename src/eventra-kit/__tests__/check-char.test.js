import { describe, it, expect } from 'vitest';
import * as CheckChar from '../check-char.js';

describe('check-char', () => {
  it('exports a module', () => {
    expect(CheckChar).toBeDefined();
  });
});

