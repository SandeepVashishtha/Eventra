import { describe, it, expect } from 'vitest';
import * as EnsureSpan from '../ensure-span.js';

describe('ensure-span', () => {
  it('exports a module', () => {
    expect(EnsureSpan).toBeDefined();
  });
});

