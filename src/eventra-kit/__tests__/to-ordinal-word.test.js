import { describe, it, expect } from 'vitest';
import * as ToOrdinalWord from '../to-ordinal-word.js';

describe('to-ordinal-word', () => {
  it('exports a module', () => {
    expect(ToOrdinalWord).toBeDefined();
  });
});

