import { describe, it, expect } from 'vitest';
import * as LettersOnly from '../letters-only.js';

describe('letters-only', () => {
  it('exports a module', () => {
    expect(LettersOnly).toBeDefined();
  });
});

