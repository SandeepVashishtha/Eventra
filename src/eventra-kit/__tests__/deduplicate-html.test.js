import { describe, it, expect } from 'vitest';
import * as DeduplicateHtml from '../deduplicate-html.js';

describe('deduplicate-html', () => {
  it('exports a module', () => {
    expect(DeduplicateHtml).toBeDefined();
  });
});

