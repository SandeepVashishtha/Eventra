import { describe, it, expect } from 'vitest';
import * as CompactObject from '../compact-object.js';

describe('compact-object', () => {
  it('exports a module', () => {
    expect(CompactObject).toBeDefined();
  });
});

