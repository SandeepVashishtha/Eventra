import { describe, it, expect } from 'vitest';
import * as HasUppercase from '../has-uppercase.js';

describe('has-uppercase', () => {
  it('exports a module', () => {
    expect(HasUppercase).toBeDefined();
  });
});

