import { describe, it, expect } from 'vitest';
import * as DedupeFile from '../dedupe-file.js';

describe('dedupe-file', () => {
  it('exports a module', () => {
    expect(DedupeFile).toBeDefined();
  });
});

