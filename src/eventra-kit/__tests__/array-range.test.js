import { describe, it, expect } from 'vitest';
import * as ArrayRange from '../array-range.js';

describe('array-range', () => {
  it('exports a module', () => {
    expect(ArrayRange).toBeDefined();
  });
});

