import { describe, it, expect } from 'vitest';
import * as ToStringSafe from '../to-string-safe.js';

describe('to-string-safe', () => {
  it('exports a module', () => {
    expect(ToStringSafe).toBeDefined();
  });
});

