import { describe, it, expect } from 'vitest';
import * as IsNumber from '../is-number.js';

describe('is-number', () => {
  it('exports a module', () => {
    expect(IsNumber).toBeDefined();
  });
});

