import { describe, it, expect } from 'vitest';
import * as DiffRect from '../diff-rect.js';

describe('diff-rect', () => {
  it('exports a module', () => {
    expect(DiffRect).toBeDefined();
  });
});

