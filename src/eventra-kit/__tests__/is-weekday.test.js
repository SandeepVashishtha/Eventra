import { describe, it, expect } from 'vitest';
import * as IsWeekday from '../is-weekday.js';

describe('is-weekday', () => {
  it('exports a module', () => {
    expect(IsWeekday).toBeDefined();
  });
});

