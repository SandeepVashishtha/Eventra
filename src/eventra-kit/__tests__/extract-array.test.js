import { describe, it, expect } from 'vitest';
import * as ExtractArray from '../extract-array.js';

describe('extract-array', () => {
  it('exports a module', () => {
    expect(ExtractArray).toBeDefined();
  });
});

