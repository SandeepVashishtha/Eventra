import { describe, it, expect } from 'vitest';
import * as CreateObject from '../create-object.js';

describe('create-object', () => {
  it('exports a module', () => {
    expect(CreateObject).toBeDefined();
  });
});

