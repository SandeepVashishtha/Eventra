import { describe, it, expect } from 'vitest';
import * as ParseIntBase from '../parse-int-base.js';

describe('parse-int-base', () => {
  it('exports a module', () => {
    expect(ParseIntBase).toBeDefined();
  });
});

