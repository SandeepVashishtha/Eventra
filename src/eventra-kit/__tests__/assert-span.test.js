import { describe, it, expect } from 'vitest';
import * as AssertSpan from '../assert-span.js';

describe('assert-span', () => {
  it('exports a module', () => {
    expect(AssertSpan).toBeDefined();
  });
});

