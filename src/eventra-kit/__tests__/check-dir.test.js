import { describe, it, expect } from 'vitest';
import * as CheckDir from '../check-dir.js';

describe('check-dir', () => {
  it('exports a module', () => {
    expect(CheckDir).toBeDefined();
  });
});

