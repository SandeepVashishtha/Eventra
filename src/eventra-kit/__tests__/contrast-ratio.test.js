import { describe, it, expect } from 'vitest';
import * as ContrastRatio from '../contrast-ratio.js';

describe('contrast-ratio', () => {
  it('exports a module', () => {
    expect(ContrastRatio).toBeDefined();
  });
});

