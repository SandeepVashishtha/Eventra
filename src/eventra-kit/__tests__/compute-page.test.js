import { describe, it, expect } from 'vitest';
import * as ComputePage from '../compute-page.js';

describe('compute-page', () => {
  it('exports a module', () => {
    expect(ComputePage).toBeDefined();
  });
});

