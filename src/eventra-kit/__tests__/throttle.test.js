import { describe, it, expect } from 'vitest';
import * as Throttle from '../throttle.js';

describe('throttle', () => {
  it('exports a module', () => {
    expect(Throttle).toBeDefined();
  });
});

