import { describe, it, expect } from 'vitest';
import * as CheckPoint from '../check-point.js';

describe('check-point', () => {
  it('exports a module', () => {
    expect(CheckPoint).toBeDefined();
  });
});

