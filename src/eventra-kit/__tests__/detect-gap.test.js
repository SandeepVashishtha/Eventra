import { describe, it, expect } from 'vitest';
import * as DetectGap from '../detect-gap.js';

describe('detect-gap', () => {
  it('exports a module', () => {
    expect(DetectGap).toBeDefined();
  });
});

