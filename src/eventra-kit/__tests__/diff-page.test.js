import { describe, it, expect } from 'vitest';
import * as DiffPage from '../diff-page.js';

describe('diff-page', () => {
  it('exports a module', () => {
    expect(DiffPage).toBeDefined();
  });
});

