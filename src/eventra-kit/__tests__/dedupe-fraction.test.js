import { describe, it, expect } from 'vitest';
import * as DedupeFraction from '../dedupe-fraction.js';

describe('dedupe-fraction', () => {
  it('exports a module', () => {
    expect(DedupeFraction).toBeDefined();
  });
});

