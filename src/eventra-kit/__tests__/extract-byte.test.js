import { describe, it, expect } from 'vitest';
import * as ExtractByte from '../extract-byte.js';

describe('extract-byte', () => {
  it('exports a module', () => {
    expect(ExtractByte).toBeDefined();
  });
});

