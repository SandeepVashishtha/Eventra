import { describe, it, expect } from 'vitest';
import * as DiffValue from '../diff-value.js';

describe('diff-value', () => {
  it('exports a module', () => {
    expect(DiffValue).toBeDefined();
  });
});

