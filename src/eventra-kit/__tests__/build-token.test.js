import { describe, it, expect } from 'vitest';
import * as BuildToken from '../build-token.js';

describe('build-token', () => {
  it('exports a module', () => {
    expect(BuildToken).toBeDefined();
  });
});

