import { describe, it, expect } from 'vitest';
import * as CountWeight from '../count-weight.js';

describe('count-weight', () => {
  it('exports a module', () => {
    expect(CountWeight).toBeDefined();
  });
});

