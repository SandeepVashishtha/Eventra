import { describe, it, expect } from 'vitest';
import * as DeduplicateTree from '../deduplicate-tree.js';

describe('deduplicate-tree', () => {
  it('exports a module', () => {
    expect(DeduplicateTree).toBeDefined();
  });
});

