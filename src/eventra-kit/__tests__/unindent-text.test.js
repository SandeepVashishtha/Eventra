import { describe, it, expect } from 'vitest';
import * as UnindentText from '../unindent-text.js';

describe('unindent-text', () => {
  it('exports a module', () => {
    expect(UnindentText).toBeDefined();
  });
});

