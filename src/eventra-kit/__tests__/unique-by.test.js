import { describe, it, expect } from 'vitest';
import * as UniqueBy from '../unique-by.js';

describe('unique-by', () => {
  it('exports a module', () => {
    expect(UniqueBy).toBeDefined();
  });
});

