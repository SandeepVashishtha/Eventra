import { describe, it, expect } from 'vitest';
import * as FilterKeys from '../filter-keys.js';

describe('filter-keys', () => {
  it('exports a module', () => {
    expect(FilterKeys).toBeDefined();
  });
});

