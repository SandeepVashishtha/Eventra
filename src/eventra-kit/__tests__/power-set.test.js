import { describe, it, expect } from 'vitest';
import * as PowerSet from '../power-set.js';

describe('power-set', () => {
  it('exports a module', () => {
    expect(PowerSet).toBeDefined();
  });
});

