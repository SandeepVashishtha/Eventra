import { describe, it, expect } from 'vitest';
import * as ToInitials from '../to-initials.js';

describe('to-initials', () => {
  it('exports a module', () => {
    expect(ToInitials).toBeDefined();
  });
});

