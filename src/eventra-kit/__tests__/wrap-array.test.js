import { describe, it, expect } from 'vitest';
import * as WrapArray from '../wrap-array.js';

describe('wrap-array', () => {
  it('exports a module', () => {
    expect(WrapArray).toBeDefined();
  });
});

