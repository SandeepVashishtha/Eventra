import { describe, it, expect } from 'vitest';
import * as LastItem from '../last-item.js';

describe('last-item', () => {
  it('exports a module', () => {
    expect(LastItem).toBeDefined();
  });
});

