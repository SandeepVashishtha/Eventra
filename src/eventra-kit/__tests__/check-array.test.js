import { describe, it, expect } from 'vitest';
import * as CheckArray from '../check-array.js';

describe('check-array', () => {
  it('exports a module', () => {
    expect(CheckArray).toBeDefined();
  });
});

