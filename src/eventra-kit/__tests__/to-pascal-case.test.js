import { describe, it, expect } from 'vitest';
import * as ToPascalCase from '../to-pascal-case.js';

describe('to-pascal-case', () => {
  it('exports a module', () => {
    expect(ToPascalCase).toBeDefined();
  });
});

