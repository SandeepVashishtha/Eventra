import { describe, it, expect } from 'vitest';
import * as DetectDelta from '../detect-delta.js';

describe('detect-delta', () => {
  it('exports a module', () => {
    expect(DetectDelta).toBeDefined();
  });
});

