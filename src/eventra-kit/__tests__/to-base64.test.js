import { describe, it, expect } from 'vitest';
import * as ToBase64 from '../to-base64.js';

describe('to-base64', () => {
  it('exports a module', () => {
    expect(ToBase64).toBeDefined();
  });
});

