import { describe, it, expect } from 'vitest';
import * as CalculateLength from '../calculate-length.js';

describe('calculate-length', () => {
  it('exports a module', () => {
    expect(CalculateLength).toBeDefined();
  });
});

