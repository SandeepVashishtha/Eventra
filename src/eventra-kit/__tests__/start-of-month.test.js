import { describe, it, expect } from 'vitest';
import * as StartOfMonth from '../start-of-month.js';

describe('start-of-month', () => {
  it('exports a module', () => {
    expect(StartOfMonth).toBeDefined();
  });
});

