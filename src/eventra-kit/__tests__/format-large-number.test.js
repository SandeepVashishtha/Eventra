import { describe, it, expect } from 'vitest';
import * as FormatLargeNumber from '../format-large-number.js';

describe('format-large-number', () => {
  it('exports a module', () => {
    expect(FormatLargeNumber).toBeDefined();
  });
});

