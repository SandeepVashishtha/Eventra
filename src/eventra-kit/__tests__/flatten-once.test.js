import { describe, it, expect } from 'vitest';
import * as FlattenOnce from '../flatten-once.js';

describe('flatten-once', () => {
  it('exports a module', () => {
    expect(FlattenOnce).toBeDefined();
  });
});

