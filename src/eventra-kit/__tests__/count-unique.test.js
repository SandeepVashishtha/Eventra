import { describe, it, expect } from 'vitest';
import * as CountUnique from '../count-unique.js';

describe('count-unique', () => {
  it('exports a module', () => {
    expect(CountUnique).toBeDefined();
  });
});

