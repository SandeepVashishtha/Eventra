import { describe, it, expect } from 'vitest';
import * as DeduplicateToken from '../deduplicate-token.js';

describe('deduplicate-token', () => {
  it('exports a module', () => {
    expect(DeduplicateToken).toBeDefined();
  });
});

