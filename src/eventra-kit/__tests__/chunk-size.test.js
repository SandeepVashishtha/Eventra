import { describe, it, expect } from 'vitest';
import * as ChunkSize from '../chunk-size.js';

describe('chunk-size', () => {
  it('exports a module', () => {
    expect(ChunkSize).toBeDefined();
  });
});

