import { describe, it, expect } from 'vitest';
import * as ComputeRank from '../compute-rank.js';

describe('compute-rank', () => {
  it('exports a module', () => {
    expect(ComputeRank).toBeDefined();
  });
});

