import { describe, it, expect } from 'vitest';
import * as ScaleValue from '../scale-value.js';

describe('scale-value', () => {
  it('exports a module', () => {
    expect(ScaleValue).toBeDefined();
  });
});

