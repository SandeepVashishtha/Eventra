import { describe, it, expect } from 'vitest';
import * as ParseIntList from '../parse-int-list.js';

describe('parse-int-list', () => {
  it('exports a module', () => {
    expect(ParseIntList).toBeDefined();
  });
});

