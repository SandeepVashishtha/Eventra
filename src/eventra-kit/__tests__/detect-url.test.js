import { describe, it, expect } from 'vitest';
import * as DetectUrl from '../detect-url.js';

describe('detect-url', () => {
  it('exports a module', () => {
    expect(DetectUrl).toBeDefined();
  });
});

