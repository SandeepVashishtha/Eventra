import { describe, it, expect } from 'vitest';
import * as DetectHeap from '../detect-heap.js';

describe('detect-heap', () => {
  it('exports a module', () => {
    expect(DetectHeap).toBeDefined();
  });
});

