import { describe, it, expect } from 'vitest';
import * as PercentChange from '../percent-change.js';

describe('percent-change', () => {
  it('exports a module', () => {
    expect(PercentChange).toBeDefined();
  });
});

