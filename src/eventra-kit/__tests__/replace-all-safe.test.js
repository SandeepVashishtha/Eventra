import { describe, it, expect } from 'vitest';
import * as ReplaceAllSafe from '../replace-all-safe.js';

describe('replace-all-safe', () => {
  it('exports a module', () => {
    expect(ReplaceAllSafe).toBeDefined();
  });
});

