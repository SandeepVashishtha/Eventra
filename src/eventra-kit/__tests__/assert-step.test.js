import { describe, it, expect } from 'vitest';
import * as AssertStep from '../assert-step.js';

describe('assert-step', () => {
  it('exports a module', () => {
    expect(AssertStep).toBeDefined();
  });
});

