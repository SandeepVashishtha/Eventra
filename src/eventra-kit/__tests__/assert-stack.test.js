import { describe, it, expect } from 'vitest';
import * as AssertStack from '../assert-stack.js';

describe('assert-stack', () => {
  it('exports a module', () => {
    expect(AssertStack).toBeDefined();
  });
});

