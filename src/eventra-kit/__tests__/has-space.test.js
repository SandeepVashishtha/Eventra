import { describe, it, expect } from 'vitest';
import * as HasSpace from '../has-space.js';

describe('has-space', () => {
  it('exports a module', () => {
    expect(HasSpace).toBeDefined();
  });
});

