import { describe, it, expect } from 'vitest';
import * as BuildWeight from '../build-weight.js';

describe('build-weight', () => {
  it('exports a module', () => {
    expect(BuildWeight).toBeDefined();
  });
});

