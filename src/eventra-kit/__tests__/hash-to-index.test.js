import { describe, it, expect } from 'vitest';
import * as HashToIndex from '../hash-to-index.js';

describe('hash-to-index', () => {
  it('exports a module', () => {
    expect(HashToIndex).toBeDefined();
  });
});

