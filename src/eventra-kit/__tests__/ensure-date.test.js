import { describe, it, expect } from 'vitest';
import * as EnsureDate from '../ensure-date.js';

describe('ensure-date', () => {
  it('exports a module', () => {
    expect(EnsureDate).toBeDefined();
  });
});

