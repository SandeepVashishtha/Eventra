import { describe, it, expect } from 'vitest';
import * as CheckPortion from '../check-portion.js';

describe('check-portion', () => {
  it('exports a module', () => {
    expect(CheckPortion).toBeDefined();
  });
});

