import { describe, it, expect } from 'vitest';
import * as DedupeRect from '../dedupe-rect.js';

describe('dedupe-rect', () => {
  it('exports a module', () => {
    expect(DedupeRect).toBeDefined();
  });
});

