import { describe, it, expect } from 'vitest';
import * as AssertWord from '../assert-word.js';

describe('assert-word', () => {
  it('exports a module', () => {
    expect(AssertWord).toBeDefined();
  });
});

