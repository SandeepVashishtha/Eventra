import { describe, it, expect } from 'vitest';
import * as FirstDifferentIndex from '../first-different-index.js';

describe('first-different-index', () => {
  it('exports a module', () => {
    expect(FirstDifferentIndex).toBeDefined();
  });
});

