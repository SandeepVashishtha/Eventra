import { describe, it, expect } from 'vitest';
import * as FirstMatch from '../first-match.js';

describe('first-match', () => {
  it('exports a module', () => {
    expect(FirstMatch).toBeDefined();
  });
});

