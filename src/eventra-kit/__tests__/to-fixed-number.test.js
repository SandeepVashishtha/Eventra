import { describe, it, expect } from 'vitest';
import * as ToFixedNumber from '../to-fixed-number.js';

describe('to-fixed-number', () => {
  it('exports a module', () => {
    expect(ToFixedNumber).toBeDefined();
  });
});

