import { describe, it, expect } from 'vitest';
import * as IndexOfAll from '../index-of-all.js';

describe('index-of-all', () => {
  it('exports a module', () => {
    expect(IndexOfAll).toBeDefined();
  });
});

