import { describe, it, expect } from 'vitest';
import * as CheckRange from '../check-range.js';

describe('check-range', () => {
  it('exports a module', () => {
    expect(CheckRange).toBeDefined();
  });
});

