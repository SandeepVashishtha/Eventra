import { describe, it, expect } from 'vitest';
import * as CheckPage from '../check-page.js';

describe('check-page', () => {
  it('exports a module', () => {
    expect(CheckPage).toBeDefined();
  });
});

