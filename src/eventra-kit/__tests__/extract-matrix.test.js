import { describe, it, expect } from 'vitest';
import * as ExtractMatrix from '../extract-matrix.js';

describe('extract-matrix', () => {
  it('exports a module', () => {
    expect(ExtractMatrix).toBeDefined();
  });
});

