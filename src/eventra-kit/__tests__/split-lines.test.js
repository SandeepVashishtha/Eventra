import { describe, it, expect } from 'vitest';
import * as SplitLines from '../split-lines.js';

describe('split-lines', () => {
  it('exports a module', () => {
    expect(SplitLines).toBeDefined();
  });
});

