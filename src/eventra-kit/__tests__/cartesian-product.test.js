import { describe, it, expect } from 'vitest';
import * as CartesianProduct from '../cartesian-product.js';

describe('cartesian-product', () => {
  it('exports a module', () => {
    expect(CartesianProduct).toBeDefined();
  });
});

