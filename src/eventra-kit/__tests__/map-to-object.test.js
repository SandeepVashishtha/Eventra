import { describe, it, expect } from 'vitest';
import * as MapToObject from '../map-to-object.js';

describe('map-to-object', () => {
  it('exports a module', () => {
    expect(MapToObject).toBeDefined();
  });
});

