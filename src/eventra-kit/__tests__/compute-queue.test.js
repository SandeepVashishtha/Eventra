import { describe, it, expect } from 'vitest';
import * as ComputeQueue from '../compute-queue.js';

describe('compute-queue', () => {
  it('exports a module', () => {
    expect(ComputeQueue).toBeDefined();
  });
});

