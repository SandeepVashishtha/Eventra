import { describe, it, expect } from 'vitest';
import * as PercentileValue from '../percentile-value.js';

describe('percentile-value', () => {
  it('exports a module', () => {
    expect(PercentileValue).toBeDefined();
  });
});

