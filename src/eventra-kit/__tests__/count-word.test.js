import { describe, it, expect } from 'vitest';
import * as CountWord from '../count-word.js';

describe('count-word', () => {
  it('exports a module', () => {
    expect(CountWord).toBeDefined();
  });
});

