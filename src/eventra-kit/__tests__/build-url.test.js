import { describe, it, expect } from 'vitest';
import * as BuildUrl from '../build-url.js';

describe('build-url', () => {
  it('exports a module', () => {
    expect(BuildUrl).toBeDefined();
  });
});

