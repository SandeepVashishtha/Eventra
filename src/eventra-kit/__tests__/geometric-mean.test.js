import { describe, it, expect } from 'vitest';
import * as GeometricMean from '../geometric-mean.js';

describe('geometric-mean', () => {
  it('exports a module', () => {
    expect(GeometricMean).toBeDefined();
  });
});

