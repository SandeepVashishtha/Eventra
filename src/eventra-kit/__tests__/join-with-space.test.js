import { describe, it, expect } from 'vitest';
import * as JoinWithSpace from '../join-with-space.js';

describe('join-with-space', () => {
  it('exports a module', () => {
    expect(JoinWithSpace).toBeDefined();
  });
});

