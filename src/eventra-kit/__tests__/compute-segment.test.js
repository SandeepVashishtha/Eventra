import { describe, it, expect } from 'vitest';
import * as ComputeSegment from '../compute-segment.js';

describe('compute-segment', () => {
  it('exports a module', () => {
    expect(ComputeSegment).toBeDefined();
  });
});

