import { describe, it, expect } from 'vitest';
import * as ElapsedTimeBetween from '../elapsed-time-between.js';

describe('elapsed-time-between', () => {
  it('exports a module', () => {
    expect(ElapsedTimeBetween).toBeDefined();
  });
});

