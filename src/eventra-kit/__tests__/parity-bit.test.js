import { describe, it, expect } from 'vitest';
import * as ParityBit from '../parity-bit.js';

describe('parity-bit', () => {
  it('exports a module', () => {
    expect(ParityBit).toBeDefined();
  });
});

