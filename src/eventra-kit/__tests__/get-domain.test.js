import { describe, it, expect } from 'vitest';
import * as GetDomain from '../get-domain.js';

describe('get-domain', () => {
  it('exports a module', () => {
    expect(GetDomain).toBeDefined();
  });
});

