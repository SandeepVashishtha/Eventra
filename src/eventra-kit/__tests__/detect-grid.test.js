import { describe, it, expect } from 'vitest';
import * as DetectGrid from '../detect-grid.js';

describe('detect-grid', () => {
  it('exports a module', () => {
    expect(DetectGrid).toBeDefined();
  });
});

