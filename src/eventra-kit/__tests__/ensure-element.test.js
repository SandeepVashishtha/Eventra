import { describe, it, expect } from 'vitest';
import * as EnsureElement from '../ensure-element.js';

describe('ensure-element', () => {
  it('exports a module', () => {
    expect(EnsureElement).toBeDefined();
  });
});

