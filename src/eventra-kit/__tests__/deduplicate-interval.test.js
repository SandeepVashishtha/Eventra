import { describe, it, expect } from 'vitest';
import * as DeduplicateInterval from '../deduplicate-interval.js';

describe('deduplicate-interval', () => {
  it('exports a module', () => {
    expect(DeduplicateInterval).toBeDefined();
  });
});

