import { describe, it, expect } from 'vitest';
import * as ClampObject from '../clamp-object.js';

describe('clamp-object', () => {
  it('exports a module', () => {
    expect(ClampObject).toBeDefined();
  });
});

