import { describe, it, expect } from 'vitest';
import * as DetectUri from '../detect-uri.js';

describe('detect-uri', () => {
  it('exports a module', () => {
    expect(DetectUri).toBeDefined();
  });
});

