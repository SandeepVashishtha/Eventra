import { describe, it, expect } from 'vitest';
import * as CheckPath from '../check-path.js';

describe('check-path', () => {
  it('exports a module', () => {
    expect(CheckPath).toBeDefined();
  });
});

