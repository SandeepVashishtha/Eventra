import { describe, it, expect } from 'vitest';
import * as GroupByValueType from '../group-by-value-type.js';

describe('group-by-value-type', () => {
  it('exports a module', () => {
    expect(GroupByValueType).toBeDefined();
  });
});

