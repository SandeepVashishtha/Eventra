import { describe, it, expect } from 'vitest';
import * as RemoveIndex from '../remove-index.js';

describe('remove-index', () => {
  it('exports a module', () => {
    expect(RemoveIndex).toBeDefined();
  });
});

