import { describe, it, expect } from 'vitest';
import * as ReverseSort from '../reverse-sort.js';

describe('reverse-sort', () => {
  it('exports a module', () => {
    expect(ReverseSort).toBeDefined();
  });
});

