import { describe, it, expect } from 'vitest';
import * as Differences from '../differences.js';

describe('differences', () => {
  it('exports a module', () => {
    expect(Differences).toBeDefined();
  });
});

