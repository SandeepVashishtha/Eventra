import { describe, it, expect } from 'vitest';
import * as QueryToJson from '../query-to-json.js';

describe('query-to-json', () => {
  it('exports a module', () => {
    expect(QueryToJson).toBeDefined();
  });
});

