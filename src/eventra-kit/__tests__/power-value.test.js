import { describe, it, expect } from 'vitest';
import * as PowerValue from '../power-value.js';

describe('power-value', () => {
  it('exports a module', () => {
    expect(PowerValue).toBeDefined();
  });
});

