import { describe, it, expect } from 'vitest';
import * as ProductArray from '../product-array.js';

describe('product-array', () => {
  it('exports a module', () => {
    expect(ProductArray).toBeDefined();
  });
});

