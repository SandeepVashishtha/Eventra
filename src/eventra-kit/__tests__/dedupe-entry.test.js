import { describe, it, expect } from 'vitest';
import * as DedupeEntry from '../dedupe-entry.js';

describe('dedupe-entry', () => {
  it('exports a module', () => {
    expect(DedupeEntry).toBeDefined();
  });
});

