import { describe, it, expect } from 'vitest';
import * as ToPathCase from '../to-path-case.js';

describe('to-path-case', () => {
  it('exports a module', () => {
    expect(ToPathCase).toBeDefined();
  });
});

