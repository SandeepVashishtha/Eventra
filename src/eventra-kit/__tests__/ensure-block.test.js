import { describe, it, expect } from 'vitest';
import * as EnsureBlock from '../ensure-block.js';

describe('ensure-block', () => {
  it('exports a module', () => {
    expect(EnsureBlock).toBeDefined();
  });
});

