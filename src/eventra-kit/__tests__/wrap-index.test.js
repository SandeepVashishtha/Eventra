import { describe, it, expect } from 'vitest';
import * as WrapIndex from '../wrap-index.js';

describe('wrap-index', () => {
  it('exports a module', () => {
    expect(WrapIndex).toBeDefined();
  });
});

