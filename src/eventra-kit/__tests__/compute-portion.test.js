import { describe, it, expect } from 'vitest';
import * as ComputePortion from '../compute-portion.js';

describe('compute-portion', () => {
  it('exports a module', () => {
    expect(ComputePortion).toBeDefined();
  });
});

