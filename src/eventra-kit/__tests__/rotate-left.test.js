import { describe, it, expect } from 'vitest';
import * as RotateLeft from '../rotate-left.js';

describe('rotate-left', () => {
  it('exports a module', () => {
    expect(RotateLeft).toBeDefined();
  });
});

