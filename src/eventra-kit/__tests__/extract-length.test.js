import { describe, it, expect } from 'vitest';
import * as ExtractLength from '../extract-length.js';

describe('extract-length', () => {
  it('exports a module', () => {
    expect(ExtractLength).toBeDefined();
  });
});

