import { describe, it, expect } from 'vitest';
import * as PadNumber from '../pad-number.js';

describe('pad-number', () => {
  it('exports a module', () => {
    expect(PadNumber).toBeDefined();
  });
});

