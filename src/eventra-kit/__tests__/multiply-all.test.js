import { describe, it, expect } from 'vitest';
import * as MultiplyAll from '../multiply-all.js';

describe('multiply-all', () => {
  it('exports a module', () => {
    expect(MultiplyAll).toBeDefined();
  });
});

