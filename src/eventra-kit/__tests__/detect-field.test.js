import { describe, it, expect } from 'vitest';
import * as DetectField from '../detect-field.js';

describe('detect-field', () => {
  it('exports a module', () => {
    expect(DetectField).toBeDefined();
  });
});

