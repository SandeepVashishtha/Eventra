import { describe, it, expect } from 'vitest';
import * as EnsureSegment from '../ensure-segment.js';

describe('ensure-segment', () => {
  it('exports a module', () => {
    expect(EnsureSegment).toBeDefined();
  });
});

