import { describe, it, expect } from 'vitest';
import * as DetectElement from '../detect-element.js';

describe('detect-element', () => {
  it('exports a module', () => {
    expect(DetectElement).toBeDefined();
  });
});

