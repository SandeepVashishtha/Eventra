import { describe, it, expect } from 'vitest';
import * as RemoveTags from '../remove-tags.js';

describe('remove-tags', () => {
  it('exports a module', () => {
    expect(RemoveTags).toBeDefined();
  });
});

