import { describe, it, expect } from 'vitest';
import * as DiffOrder from '../diff-order.js';

describe('diff-order', () => {
  it('exports a module', () => {
    expect(DiffOrder).toBeDefined();
  });
});

