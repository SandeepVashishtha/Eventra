import { describe, it, expect } from 'vitest';
import * as HasDigit from '../has-digit.js';

describe('has-digit', () => {
  it('exports a module', () => {
    expect(HasDigit).toBeDefined();
  });
});

