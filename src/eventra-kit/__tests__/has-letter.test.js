import { describe, it, expect } from 'vitest';
import * as HasLetter from '../has-letter.js';

describe('has-letter', () => {
  it('exports a module', () => {
    expect(HasLetter).toBeDefined();
  });
});

