import { describe, it, expect } from 'vitest';
import * as SortByCount from '../sort-by-count.js';

describe('sort-by-count', () => {
  it('exports a module', () => {
    expect(SortByCount).toBeDefined();
  });
});

