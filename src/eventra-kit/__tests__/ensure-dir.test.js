import { describe, it, expect } from 'vitest';
import * as EnsureDir from '../ensure-dir.js';

describe('ensure-dir', () => {
  it('exports a module', () => {
    expect(EnsureDir).toBeDefined();
  });
});

