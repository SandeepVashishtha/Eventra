import { describe, it, expect } from 'vitest';
import * as GetScrollTop from '../get-scroll-top.js';

describe('get-scroll-top', () => {
  it('exports a module', () => {
    expect(GetScrollTop).toBeDefined();
  });
});

