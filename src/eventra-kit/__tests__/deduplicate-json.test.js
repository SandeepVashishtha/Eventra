import { describe, it, expect } from 'vitest';
import * as DeduplicateJson from '../deduplicate-json.js';

describe('deduplicate-json', () => {
  it('exports a module', () => {
    expect(DeduplicateJson).toBeDefined();
  });
});

