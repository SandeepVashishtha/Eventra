import { describe, it, expect } from 'vitest';
import * as LogBase10 from '../log-base10.js';

describe('log-base10', () => {
  it('exports a module', () => {
    expect(LogBase10).toBeDefined();
  });
});

