import { describe, it, expect } from 'vitest';
import * as ToEpochMilliseconds from '../to-epoch-milliseconds.js';

describe('to-epoch-milliseconds', () => {
  it('exports a module', () => {
    expect(ToEpochMilliseconds).toBeDefined();
  });
});

