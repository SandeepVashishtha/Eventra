import { describe, it, expect } from 'vitest';
import * as DetectInterval from '../detect-interval.js';

describe('detect-interval', () => {
  it('exports a module', () => {
    expect(DetectInterval).toBeDefined();
  });
});

