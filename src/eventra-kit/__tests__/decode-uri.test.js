import { describe, it, expect } from 'vitest';
import * as DecodeUri from '../decode-uri.js';

describe('decode-uri', () => {
  it('exports a module', () => {
    expect(DecodeUri).toBeDefined();
  });
});

