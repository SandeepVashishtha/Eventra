import { describe, it, expect } from 'vitest';
import * as StringToArray from '../string-to-array.js';

describe('string-to-array', () => {
  it('exports a module', () => {
    expect(StringToArray).toBeDefined();
  });
});

