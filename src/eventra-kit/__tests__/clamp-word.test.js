import { describe, it, expect } from 'vitest';
import * as ClampWord from '../clamp-word.js';

describe('clamp-word', () => {
  it('exports a module', () => {
    expect(ClampWord).toBeDefined();
  });
});

