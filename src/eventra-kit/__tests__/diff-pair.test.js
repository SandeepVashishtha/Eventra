import { describe, it, expect } from 'vitest';
import * as DiffPair from '../diff-pair.js';

describe('diff-pair', () => {
  it('exports a module', () => {
    expect(DiffPair).toBeDefined();
  });
});

