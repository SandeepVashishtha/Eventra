import { describe, it, expect } from 'vitest';
import * as CombineUrl from '../combine-url.js';

describe('combine-url', () => {
  it('exports a module', () => {
    expect(CombineUrl).toBeDefined();
  });
});

