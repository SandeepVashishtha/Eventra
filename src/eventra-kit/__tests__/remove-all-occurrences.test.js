import { describe, it, expect } from 'vitest';
import * as RemoveAllOccurrences from '../remove-all-occurrences.js';

describe('remove-all-occurrences', () => {
  it('exports a module', () => {
    expect(RemoveAllOccurrences).toBeDefined();
  });
});

