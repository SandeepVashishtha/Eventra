import { describe, it, expect } from 'vitest';
import * as MergeByKey from '../merge-by-key.js';

describe('merge-by-key', () => {
  it('exports a module', () => {
    expect(MergeByKey).toBeDefined();
  });
});

