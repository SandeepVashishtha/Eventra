import { describe, it, expect } from 'vitest';
import * as ToPercent from '../to-percent.js';

describe('to-percent', () => {
  it('exports a module', () => {
    expect(ToPercent).toBeDefined();
  });
});

