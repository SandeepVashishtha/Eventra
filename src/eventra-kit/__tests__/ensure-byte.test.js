import { describe, it, expect } from 'vitest';
import * as EnsureByte from '../ensure-byte.js';

describe('ensure-byte', () => {
  it('exports a module', () => {
    expect(EnsureByte).toBeDefined();
  });
});

