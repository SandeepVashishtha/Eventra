import { describe, it, expect } from 'vitest';
import * as InterquartileRange from '../interquartile-range.js';

describe('interquartile-range', () => {
  it('exports a module', () => {
    expect(InterquartileRange).toBeDefined();
  });
});

