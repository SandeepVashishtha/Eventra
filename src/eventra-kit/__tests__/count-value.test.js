import { describe, it, expect } from 'vitest';
import * as CountValue from '../count-value.js';

describe('count-value', () => {
  it('exports a module', () => {
    expect(CountValue).toBeDefined();
  });
});

