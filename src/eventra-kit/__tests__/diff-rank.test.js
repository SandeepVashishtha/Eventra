import { describe, it, expect } from 'vitest';
import * as DiffRank from '../diff-rank.js';

describe('diff-rank', () => {
  it('exports a module', () => {
    expect(DiffRank).toBeDefined();
  });
});

