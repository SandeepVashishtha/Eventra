import { describe, it, expect } from 'vitest';
import * as Ipv4Valid from '../ipv4-valid.js';

describe('ipv4-valid', () => {
  it('exports a module', () => {
    expect(Ipv4Valid).toBeDefined();
  });
});

