import { describe, it, expect } from 'vitest';
import * as EnsureSlice from '../ensure-slice.js';

describe('ensure-slice', () => {
  it('exports a module', () => {
    expect(EnsureSlice).toBeDefined();
  });
});

