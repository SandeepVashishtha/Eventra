import { describe, it, expect } from 'vitest';
import * as MinTwoValues from '../min-two-values.js';

describe('min-two-values', () => {
  it('exports a module', () => {
    expect(MinTwoValues).toBeDefined();
  });
});

