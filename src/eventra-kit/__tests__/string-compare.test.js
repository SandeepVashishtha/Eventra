import { describe, it, expect } from 'vitest';
import * as StringCompare from '../string-compare.js';

describe('string-compare', () => {
  it('exports a module', () => {
    expect(StringCompare).toBeDefined();
  });
});

