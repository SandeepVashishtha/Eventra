import { describe, it, expect } from 'vitest';
import * as IsValidPhone from '../is-valid-phone.js';

describe('is-valid-phone', () => {
  it('exports a module', () => {
    expect(IsValidPhone).toBeDefined();
  });
});

