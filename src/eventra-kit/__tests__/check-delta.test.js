import { describe, it, expect } from 'vitest';
import * as CheckDelta from '../check-delta.js';

describe('check-delta', () => {
  it('exports a module', () => {
    expect(CheckDelta).toBeDefined();
  });
});

