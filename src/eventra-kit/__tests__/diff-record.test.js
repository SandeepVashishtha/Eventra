import { describe, it, expect } from 'vitest';
import * as DiffRecord from '../diff-record.js';

describe('diff-record', () => {
  it('exports a module', () => {
    expect(DiffRecord).toBeDefined();
  });
});

