import { describe, it, expect } from 'vitest';
import * as GetDivisors from '../get-divisors.js';

describe('get-divisors', () => {
  it('exports a module', () => {
    expect(GetDivisors).toBeDefined();
  });
});

