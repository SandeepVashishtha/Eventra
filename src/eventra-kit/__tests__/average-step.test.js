import { describe, it, expect } from 'vitest';
import * as AverageStep from '../average-step.js';

describe('average-step', () => {
  it('exports a module', () => {
    expect(AverageStep).toBeDefined();
  });
});

