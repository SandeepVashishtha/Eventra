import { describe, it, expect } from 'vitest';
import * as LowercaseFirst from '../lowercase-first.js';

describe('lowercase-first', () => {
  it('exports a module', () => {
    expect(LowercaseFirst).toBeDefined();
  });
});

