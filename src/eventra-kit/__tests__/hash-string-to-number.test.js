import { describe, it, expect } from 'vitest';
import * as HashStringToNumber from '../hash-string-to-number.js';

describe('hash-string-to-number', () => {
  it('exports a module', () => {
    expect(HashStringToNumber).toBeDefined();
  });
});

