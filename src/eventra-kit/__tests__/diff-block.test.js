import { describe, it, expect } from 'vitest';
import * as DiffBlock from '../diff-block.js';

describe('diff-block', () => {
  it('exports a module', () => {
    expect(DiffBlock).toBeDefined();
  });
});

