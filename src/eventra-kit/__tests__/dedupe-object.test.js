import { describe, it, expect } from 'vitest';
import * as DedupeObject from '../dedupe-object.js';

describe('dedupe-object', () => {
  it('exports a module', () => {
    expect(DedupeObject).toBeDefined();
  });
});

