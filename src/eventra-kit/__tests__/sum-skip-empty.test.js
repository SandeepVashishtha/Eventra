import { describe, it, expect } from 'vitest';
import * as SumSkipEmpty from '../sum-skip-empty.js';

describe('sum-skip-empty', () => {
  it('exports a module', () => {
    expect(SumSkipEmpty).toBeDefined();
  });
});

