import { describe, it, expect } from 'vitest';
import * as DetectBox from '../detect-box.js';

describe('detect-box', () => {
  it('exports a module', () => {
    expect(DetectBox).toBeDefined();
  });
});

