import { describe, it, expect } from 'vitest';
import * as ArrayToObject from '../array-to-object.js';

describe('array-to-object', () => {
  it('exports a module', () => {
    expect(ArrayToObject).toBeDefined();
  });
});

