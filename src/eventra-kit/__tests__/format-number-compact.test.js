import { describe, it, expect } from 'vitest';
import * as FormatNumberCompact from '../format-number-compact.js';

describe('format-number-compact', () => {
  it('exports a module', () => {
    expect(FormatNumberCompact).toBeDefined();
  });
});

