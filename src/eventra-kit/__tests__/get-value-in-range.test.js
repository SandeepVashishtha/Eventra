import { describe, it, expect } from 'vitest';
import * as GetValueInRange from '../get-value-in-range.js';

describe('get-value-in-range', () => {
  it('exports a module', () => {
    expect(GetValueInRange).toBeDefined();
  });
});

