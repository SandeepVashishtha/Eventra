import { describe, it, expect } from 'vitest';
import * as PartitionArray from '../partition-array.js';

describe('partition-array', () => {
  it('exports a module', () => {
    expect(PartitionArray).toBeDefined();
  });
});

