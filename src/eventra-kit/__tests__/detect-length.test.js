import { describe, it, expect } from 'vitest';
import * as DetectLength from '../detect-length.js';

describe('detect-length', () => {
  it('exports a module', () => {
    expect(DetectLength).toBeDefined();
  });
});

