import { describe, it, expect } from 'vitest';
import * as DetectCircle from '../detect-circle.js';

describe('detect-circle', () => {
  it('exports a module', () => {
    expect(DetectCircle).toBeDefined();
  });
});

