import { describe, it, expect } from 'vitest';
import * as EnsureEdge from '../ensure-edge.js';

describe('ensure-edge', () => {
  it('exports a module', () => {
    expect(EnsureEdge).toBeDefined();
  });
});

