import { describe, it, expect } from 'vitest';
import * as SafeJsonParse from '../safe-json-parse.js';

describe('safe-json-parse', () => {
  it('exports a module', () => {
    expect(SafeJsonParse).toBeDefined();
  });
});

