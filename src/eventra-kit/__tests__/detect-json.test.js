import { describe, it, expect } from 'vitest';
import * as DetectJson from '../detect-json.js';

describe('detect-json', () => {
  it('exports a module', () => {
    expect(DetectJson).toBeDefined();
  });
});

