import { describe, it, expect } from 'vitest';
import * as SumRange from '../sum-range.js';

describe('sum-range', () => {
  it('exports a module', () => {
    expect(SumRange).toBeDefined();
  });
});

