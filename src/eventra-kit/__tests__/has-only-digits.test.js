import { describe, it, expect } from 'vitest';
import * as HasOnlyDigits from '../has-only-digits.js';

describe('has-only-digits', () => {
  it('exports a module', () => {
    expect(HasOnlyDigits).toBeDefined();
  });
});

