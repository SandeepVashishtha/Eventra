import { describe, it, expect } from 'vitest';
import * as SymmetricDifference from '../symmetric-difference.js';

describe('symmetric-difference', () => {
  it('exports a module', () => {
    expect(SymmetricDifference).toBeDefined();
  });
});

