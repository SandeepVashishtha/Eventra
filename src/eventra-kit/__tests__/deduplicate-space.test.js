import { describe, it, expect } from 'vitest';
import * as DeduplicateSpace from '../deduplicate-space.js';

describe('deduplicate-space', () => {
  it('exports a module', () => {
    expect(DeduplicateSpace).toBeDefined();
  });
});

