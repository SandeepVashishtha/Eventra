import { describe, it, expect } from 'vitest';
import * as ToCurrency from '../to-currency.js';

describe('to-currency', () => {
  it('exports a module', () => {
    expect(ToCurrency).toBeDefined();
  });
});

