import { describe, it, expect } from 'vitest';
import * as NegateNumber from '../negate-number.js';

describe('negate-number', () => {
  it('exports a module', () => {
    expect(NegateNumber).toBeDefined();
  });
});

