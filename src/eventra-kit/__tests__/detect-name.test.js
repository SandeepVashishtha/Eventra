import { describe, it, expect } from 'vitest';
import * as DetectName from '../detect-name.js';

describe('detect-name', () => {
  it('exports a module', () => {
    expect(DetectName).toBeDefined();
  });
});

