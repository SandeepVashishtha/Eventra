import { describe, it, expect } from 'vitest';
import * as ToConstantCase from '../to-constant-case.js';

describe('to-constant-case', () => {
  it('exports a module', () => {
    expect(ToConstantCase).toBeDefined();
  });
});

