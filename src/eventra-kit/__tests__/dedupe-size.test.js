import { describe, it, expect } from 'vitest';
import * as DedupeSize from '../dedupe-size.js';

describe('dedupe-size', () => {
  it('exports a module', () => {
    expect(DedupeSize).toBeDefined();
  });
});

