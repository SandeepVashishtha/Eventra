import { describe, it, expect } from 'vitest';
import * as GroupConsecutiveBy from '../group-consecutive-by.js';

describe('group-consecutive-by', () => {
  it('exports a module', () => {
    expect(GroupConsecutiveBy).toBeDefined();
  });
});

