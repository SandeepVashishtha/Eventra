import { describe, it, expect } from 'vitest';
import * as PrefixLines from '../prefix-lines.js';

describe('prefix-lines', () => {
  it('exports a module', () => {
    expect(PrefixLines).toBeDefined();
  });
});

