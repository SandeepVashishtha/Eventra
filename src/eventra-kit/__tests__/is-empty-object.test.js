import { describe, it, expect } from 'vitest';
import * as IsEmptyObject from '../is-empty-object.js';

describe('is-empty-object', () => {
  it('exports a module', () => {
    expect(IsEmptyObject).toBeDefined();
  });
});

