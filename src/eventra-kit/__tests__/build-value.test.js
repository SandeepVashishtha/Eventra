import { describe, it, expect } from 'vitest';
import * as BuildValue from '../build-value.js';

describe('build-value', () => {
  it('exports a module', () => {
    expect(BuildValue).toBeDefined();
  });
});

