import { describe, it, expect } from 'vitest';
import * as ConvertTree from '../convert-tree.js';

describe('convert-tree', () => {
  it('exports a module', () => {
    expect(ConvertTree).toBeDefined();
  });
});

