import { describe, it, expect } from 'vitest';
import * as GetInitials from '../get-initials.js';

describe('get-initials', () => {
  it('exports a module', () => {
    expect(GetInitials).toBeDefined();
  });
});

