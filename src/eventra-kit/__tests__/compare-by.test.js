import { describe, it, expect } from 'vitest';
import * as CompareBy from '../compare-by.js';

describe('compare-by', () => {
  it('exports a module', () => {
    expect(CompareBy).toBeDefined();
  });
});

