import { describe, it, expect } from 'vitest';
import * as MultiplyEach from '../multiply-each.js';

describe('multiply-each', () => {
  it('exports a module', () => {
    expect(MultiplyEach).toBeDefined();
  });
});

