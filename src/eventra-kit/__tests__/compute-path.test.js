import { describe, it, expect } from 'vitest';
import * as ComputePath from '../compute-path.js';

describe('compute-path', () => {
  it('exports a module', () => {
    expect(ComputePath).toBeDefined();
  });
});

