import { describe, it, expect } from 'vitest';
import * as ComputeWeight from '../compute-weight.js';

describe('compute-weight', () => {
  it('exports a module', () => {
    expect(ComputeWeight).toBeDefined();
  });
});

