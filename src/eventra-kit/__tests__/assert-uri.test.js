import { describe, it, expect } from 'vitest';
import * as AssertUri from '../assert-uri.js';

describe('assert-uri', () => {
  it('exports a module', () => {
    expect(AssertUri).toBeDefined();
  });
});

