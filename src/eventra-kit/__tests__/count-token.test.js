import { describe, it, expect } from 'vitest';
import * as CountToken from '../count-token.js';

describe('count-token', () => {
  it('exports a module', () => {
    expect(CountToken).toBeDefined();
  });
});

