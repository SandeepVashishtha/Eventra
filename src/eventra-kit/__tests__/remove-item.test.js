import { describe, it, expect } from 'vitest';
import * as RemoveItem from '../remove-item.js';

describe('remove-item', () => {
  it('exports a module', () => {
    expect(RemoveItem).toBeDefined();
  });
});

