import { describe, it, expect } from 'vitest';
import * as DetectChunk from '../detect-chunk.js';

describe('detect-chunk', () => {
  it('exports a module', () => {
    expect(DetectChunk).toBeDefined();
  });
});

