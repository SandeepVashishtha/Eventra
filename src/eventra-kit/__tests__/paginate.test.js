import { describe, it, expect } from 'vitest';
import * as Paginate from '../paginate.js';

describe('paginate', () => {
  it('exports a module', () => {
    expect(Paginate).toBeDefined();
  });
});

