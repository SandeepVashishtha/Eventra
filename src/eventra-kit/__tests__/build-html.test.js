import { describe, it, expect } from 'vitest';
import * as BuildHtml from '../build-html.js';

describe('build-html', () => {
  it('exports a module', () => {
    expect(BuildHtml).toBeDefined();
  });
});

