import { describe, it, expect } from 'vitest';
import * as ToInteger from '../to-integer.js';

describe('to-integer', () => {
  it('exports a module', () => {
    expect(ToInteger).toBeDefined();
  });
});

