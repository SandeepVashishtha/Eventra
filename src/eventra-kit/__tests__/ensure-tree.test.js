import { describe, it, expect } from 'vitest';
import * as EnsureTree from '../ensure-tree.js';

describe('ensure-tree', () => {
  it('exports a module', () => {
    expect(EnsureTree).toBeDefined();
  });
});

