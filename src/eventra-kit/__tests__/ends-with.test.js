import { describe, it, expect } from 'vitest';
import * as EndsWith from '../ends-with.js';

describe('ends-with', () => {
  it('exports a module', () => {
    expect(EndsWith).toBeDefined();
  });
});

