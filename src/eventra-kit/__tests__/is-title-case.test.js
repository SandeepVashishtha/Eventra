import { describe, it, expect } from 'vitest';
import * as IsTitleCase from '../is-title-case.js';

describe('is-title-case', () => {
  it('exports a module', () => {
    expect(IsTitleCase).toBeDefined();
  });
});

