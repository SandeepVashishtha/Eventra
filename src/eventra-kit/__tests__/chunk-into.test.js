import { describe, it, expect } from 'vitest';
import * as ChunkInto from '../chunk-into.js';

describe('chunk-into', () => {
  it('exports a module', () => {
    expect(ChunkInto).toBeDefined();
  });
});

