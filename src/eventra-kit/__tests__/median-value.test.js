import { describe, it, expect } from 'vitest';
import * as MedianValue from '../median-value.js';

describe('median-value', () => {
  it('exports a module', () => {
    expect(MedianValue).toBeDefined();
  });
});

