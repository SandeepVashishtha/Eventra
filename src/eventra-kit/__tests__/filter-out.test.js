import { describe, it, expect } from 'vitest';
import * as FilterOut from '../filter-out.js';

describe('filter-out', () => {
  it('exports a module', () => {
    expect(FilterOut).toBeDefined();
  });
});

