import { describe, it, expect } from 'vitest';
import * as StringToList from '../string-to-list.js';

describe('string-to-list', () => {
  it('exports a module', () => {
    expect(StringToList).toBeDefined();
  });
});

