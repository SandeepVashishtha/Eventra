import { describe, it, expect } from 'vitest';
import * as BuildUri from '../build-uri.js';

describe('build-uri', () => {
  it('exports a module', () => {
    expect(BuildUri).toBeDefined();
  });
});

