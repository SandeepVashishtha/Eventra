import { describe, it, expect } from 'vitest';
import * as CheckPair from '../check-pair.js';

describe('check-pair', () => {
  it('exports a module', () => {
    expect(CheckPair).toBeDefined();
  });
});

