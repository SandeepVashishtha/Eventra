import { describe, it, expect } from 'vitest';
import * as AverageSpace from '../average-space.js';

describe('average-space', () => {
  it('exports a module', () => {
    expect(AverageSpace).toBeDefined();
  });
});

