import { describe, it, expect } from 'vitest';
import * as IsFiniteValue from '../is-finite-value.js';

describe('is-finite-value', () => {
  it('exports a module', () => {
    expect(IsFiniteValue).toBeDefined();
  });
});

