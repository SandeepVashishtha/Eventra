import { describe, it, expect } from 'vitest';
import * as EnsureGap from '../ensure-gap.js';

describe('ensure-gap', () => {
  it('exports a module', () => {
    expect(EnsureGap).toBeDefined();
  });
});

