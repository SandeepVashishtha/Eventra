import { describe, it, expect } from 'vitest';
import * as DetectText from '../detect-text.js';

describe('detect-text', () => {
  it('exports a module', () => {
    expect(DetectText).toBeDefined();
  });
});

