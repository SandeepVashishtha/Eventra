import { describe, it, expect } from 'vitest';
import * as DiffWeight from '../diff-weight.js';

describe('diff-weight', () => {
  it('exports a module', () => {
    expect(DiffWeight).toBeDefined();
  });
});

