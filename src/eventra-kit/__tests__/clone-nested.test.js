import { describe, it, expect } from 'vitest';
import * as CloneNested from '../clone-nested.js';

describe('clone-nested', () => {
  it('exports a module', () => {
    expect(CloneNested).toBeDefined();
  });
});

