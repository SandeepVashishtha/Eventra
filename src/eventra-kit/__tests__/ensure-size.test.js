import { describe, it, expect } from 'vitest';
import * as EnsureSize from '../ensure-size.js';

describe('ensure-size', () => {
  it('exports a module', () => {
    expect(EnsureSize).toBeDefined();
  });
});

