import { describe, it, expect } from 'vitest';
import * as RemoveDigits from '../remove-digits.js';

describe('remove-digits', () => {
  it('exports a module', () => {
    expect(RemoveDigits).toBeDefined();
  });
});

