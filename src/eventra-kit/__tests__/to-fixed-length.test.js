import { describe, it, expect } from 'vitest';
import * as ToFixedLength from '../to-fixed-length.js';

describe('to-fixed-length', () => {
  it('exports a module', () => {
    expect(ToFixedLength).toBeDefined();
  });
});

