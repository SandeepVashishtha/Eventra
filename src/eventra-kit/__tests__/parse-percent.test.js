import { describe, it, expect } from 'vitest';
import * as ParsePercent from '../parse-percent.js';

describe('parse-percent', () => {
  it('exports a module', () => {
    expect(ParsePercent).toBeDefined();
  });
});

