import { describe, it, expect } from 'vitest';
import * as CreateByte from '../create-byte.js';

describe('create-byte', () => {
  it('exports a module', () => {
    expect(CreateByte).toBeDefined();
  });
});

