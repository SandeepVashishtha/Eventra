import { describe, it, expect } from 'vitest';
import * as CountUrl from '../count-url.js';

describe('count-url', () => {
  it('exports a module', () => {
    expect(CountUrl).toBeDefined();
  });
});

