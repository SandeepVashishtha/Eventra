import { describe, it, expect } from 'vitest';
import * as ClampXml from '../clamp-xml.js';

describe('clamp-xml', () => {
  it('exports a module', () => {
    expect(ClampXml).toBeDefined();
  });
});

