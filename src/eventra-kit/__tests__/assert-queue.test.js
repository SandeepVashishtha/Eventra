import { describe, it, expect } from 'vitest';
import * as AssertQueue from '../assert-queue.js';

describe('assert-queue', () => {
  it('exports a module', () => {
    expect(AssertQueue).toBeDefined();
  });
});

