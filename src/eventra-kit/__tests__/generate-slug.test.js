import { describe, it, expect } from 'vitest';
import * as GenerateSlug from '../generate-slug.js';

describe('generate-slug', () => {
  it('exports a module', () => {
    expect(GenerateSlug).toBeDefined();
  });
});

