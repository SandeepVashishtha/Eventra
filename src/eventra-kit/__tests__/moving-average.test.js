import { describe, it, expect } from 'vitest';
import * as MovingAverage from '../moving-average.js';

describe('moving-average', () => {
  it('exports a module', () => {
    expect(MovingAverage).toBeDefined();
  });
});

