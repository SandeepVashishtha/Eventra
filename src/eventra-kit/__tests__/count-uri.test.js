import { describe, it, expect } from 'vitest';
import * as CountUri from '../count-uri.js';

describe('count-uri', () => {
  it('exports a module', () => {
    expect(CountUri).toBeDefined();
  });
});

