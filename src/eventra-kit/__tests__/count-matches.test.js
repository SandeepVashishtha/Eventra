import { describe, it, expect } from 'vitest';
import * as CountMatches from '../count-matches.js';

describe('count-matches', () => {
  it('exports a module', () => {
    expect(CountMatches).toBeDefined();
  });
});

