import { describe, it, expect } from 'vitest';
import * as ZipArrays from '../zip-arrays.js';

describe('zip-arrays', () => {
  it('exports a module', () => {
    expect(ZipArrays).toBeDefined();
  });
});

