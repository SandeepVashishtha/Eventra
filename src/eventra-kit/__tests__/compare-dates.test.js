import { describe, it, expect } from 'vitest';
import * as CompareDates from '../compare-dates.js';

describe('compare-dates', () => {
  it('exports a module', () => {
    expect(CompareDates).toBeDefined();
  });
});

