import { describe, it, expect } from 'vitest';
import * as ChunkStack from '../chunk-stack.js';

describe('chunk-stack', () => {
  it('exports a module', () => {
    expect(ChunkStack).toBeDefined();
  });
});

