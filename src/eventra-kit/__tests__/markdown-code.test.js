import { describe, it, expect } from 'vitest';
import * as MarkdownCode from '../markdown-code.js';

describe('markdown-code', () => {
  it('exports a module', () => {
    expect(MarkdownCode).toBeDefined();
  });
});

