import { describe, it, expect } from 'vitest';
import * as DetectItem from '../detect-item.js';

describe('detect-item', () => {
  it('exports a module', () => {
    expect(DetectItem).toBeDefined();
  });
});

