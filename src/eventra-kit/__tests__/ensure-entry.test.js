import { describe, it, expect } from 'vitest';
import * as EnsureEntry from '../ensure-entry.js';

describe('ensure-entry', () => {
  it('exports a module', () => {
    expect(EnsureEntry).toBeDefined();
  });
});

