import { describe, it, expect } from 'vitest';
import * as WrapString from '../wrap-string.js';

describe('wrap-string', () => {
  it('exports a module', () => {
    expect(WrapString).toBeDefined();
  });
});

