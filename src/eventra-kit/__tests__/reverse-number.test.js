import { describe, it, expect } from 'vitest';
import * as ReverseNumber from '../reverse-number.js';

describe('reverse-number', () => {
  it('exports a module', () => {
    expect(ReverseNumber).toBeDefined();
  });
});

