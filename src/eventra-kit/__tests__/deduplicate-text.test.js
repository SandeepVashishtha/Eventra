import { describe, it, expect } from 'vitest';
import * as DeduplicateText from '../deduplicate-text.js';

describe('deduplicate-text', () => {
  it('exports a module', () => {
    expect(DeduplicateText).toBeDefined();
  });
});

