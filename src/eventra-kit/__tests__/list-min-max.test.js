import { describe, it, expect } from 'vitest';
import * as ListMinMax from '../list-min-max.js';

describe('list-min-max', () => {
  it('exports a module', () => {
    expect(ListMinMax).toBeDefined();
  });
});

