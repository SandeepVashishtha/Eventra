import { describe, it, expect } from 'vitest';
import * as EnsureBox from '../ensure-box.js';

describe('ensure-box', () => {
  it('exports a module', () => {
    expect(EnsureBox).toBeDefined();
  });
});

