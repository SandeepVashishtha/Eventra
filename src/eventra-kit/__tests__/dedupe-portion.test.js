import { describe, it, expect } from 'vitest';
import * as DedupePortion from '../dedupe-portion.js';

describe('dedupe-portion', () => {
  it('exports a module', () => {
    expect(DedupePortion).toBeDefined();
  });
});

