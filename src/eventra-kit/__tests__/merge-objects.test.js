import { describe, it, expect } from 'vitest';
import * as MergeObjects from '../merge-objects.js';

describe('merge-objects', () => {
  it('exports a module', () => {
    expect(MergeObjects).toBeDefined();
  });
});

