import { describe, it, expect } from 'vitest';
import * as DetectLeaf from '../detect-leaf.js';

describe('detect-leaf', () => {
  it('exports a module', () => {
    expect(DetectLeaf).toBeDefined();
  });
});

