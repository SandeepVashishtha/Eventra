import { describe, it, expect } from 'vitest';
import * as GroupByKeyCount from '../group-by-key-count.js';

describe('group-by-key-count', () => {
  it('exports a module', () => {
    expect(GroupByKeyCount).toBeDefined();
  });
});

