import { describe, it, expect } from 'vitest';
import * as CreateBlock from '../create-block.js';

describe('create-block', () => {
  it('exports a module', () => {
    expect(CreateBlock).toBeDefined();
  });
});

