import { describe, it, expect } from 'vitest';
import * as ComputeRecord from '../compute-record.js';

describe('compute-record', () => {
  it('exports a module', () => {
    expect(ComputeRecord).toBeDefined();
  });
});

