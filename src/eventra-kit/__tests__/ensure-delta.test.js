import { describe, it, expect } from 'vitest';
import * as EnsureDelta from '../ensure-delta.js';

describe('ensure-delta', () => {
  it('exports a module', () => {
    expect(EnsureDelta).toBeDefined();
  });
});

