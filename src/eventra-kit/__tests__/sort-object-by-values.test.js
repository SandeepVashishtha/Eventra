import { describe, it, expect } from 'vitest';
import * as SortObjectByValues from '../sort-object-by-values.js';

describe('sort-object-by-values', () => {
  it('exports a module', () => {
    expect(SortObjectByValues).toBeDefined();
  });
});

