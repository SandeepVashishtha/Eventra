import { describe, it, expect } from 'vitest';
import * as CalculateSize from '../calculate-size.js';

describe('calculate-size', () => {
  it('exports a module', () => {
    expect(CalculateSize).toBeDefined();
  });
});

