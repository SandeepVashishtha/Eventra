import { describe, it, expect } from 'vitest';
import * as UniqueValues from '../unique-values.js';

describe('unique-values', () => {
  it('exports a module', () => {
    expect(UniqueValues).toBeDefined();
  });
});

