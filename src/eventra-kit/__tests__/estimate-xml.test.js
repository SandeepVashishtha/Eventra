import { describe, it, expect } from 'vitest';
import * as EstimateXml from '../estimate-xml.js';

describe('estimate-xml', () => {
  it('exports a module', () => {
    expect(EstimateXml).toBeDefined();
  });
});

