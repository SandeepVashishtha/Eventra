import { describe, it, expect } from 'vitest';
import * as AverageEdge from '../average-edge.js';

describe('average-edge', () => {
  it('exports a module', () => {
    expect(AverageEdge).toBeDefined();
  });
});

