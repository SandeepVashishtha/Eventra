import { describe, it, expect } from 'vitest';
import * as DedupeRange from '../dedupe-range.js';

describe('dedupe-range', () => {
  it('exports a module', () => {
    expect(DedupeRange).toBeDefined();
  });
});

