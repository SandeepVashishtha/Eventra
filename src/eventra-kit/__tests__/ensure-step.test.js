import { describe, it, expect } from 'vitest';
import * as EnsureStep from '../ensure-step.js';

describe('ensure-step', () => {
  it('exports a module', () => {
    expect(EnsureStep).toBeDefined();
  });
});

