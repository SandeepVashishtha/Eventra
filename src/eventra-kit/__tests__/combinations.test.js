import { describe, it, expect } from 'vitest';
import * as Combinations from '../combinations.js';

describe('combinations', () => {
  it('exports a module', () => {
    expect(Combinations).toBeDefined();
  });
});

