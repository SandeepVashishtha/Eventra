import { describe, it, expect } from 'vitest';
import * as MaskString from '../mask-string.js';

describe('mask-string', () => {
  it('exports a module', () => {
    expect(MaskString).toBeDefined();
  });
});

