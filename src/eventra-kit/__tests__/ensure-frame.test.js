import { describe, it, expect } from 'vitest';
import * as EnsureFrame from '../ensure-frame.js';

describe('ensure-frame', () => {
  it('exports a module', () => {
    expect(EnsureFrame).toBeDefined();
  });
});

