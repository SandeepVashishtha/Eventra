import { describe, it, expect } from 'vitest';
import * as GeneratePermutations from '../generate-permutations.js';

describe('generate-permutations', () => {
  it('exports a module', () => {
    expect(GeneratePermutations).toBeDefined();
  });
});

