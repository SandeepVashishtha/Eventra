import { describe, it, expect } from 'vitest';
import * as IsMorningTime from '../is-morning-time.js';

describe('is-morning-time', () => {
  it('exports a module', () => {
    expect(IsMorningTime).toBeDefined();
  });
});

