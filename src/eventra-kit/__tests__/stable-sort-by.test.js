import { describe, it, expect } from 'vitest';
import * as StableSortBy from '../stable-sort-by.js';

describe('stable-sort-by', () => {
  it('exports a module', () => {
    expect(StableSortBy).toBeDefined();
  });
});

