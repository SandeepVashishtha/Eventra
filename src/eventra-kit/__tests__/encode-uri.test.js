import { describe, it, expect } from 'vitest';
import * as EncodeUri from '../encode-uri.js';

describe('encode-uri', () => {
  it('exports a module', () => {
    expect(EncodeUri).toBeDefined();
  });
});

