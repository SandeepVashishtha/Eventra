import { describe, it, expect } from 'vitest';
import * as ComputePair from '../compute-pair.js';

describe('compute-pair', () => {
  it('exports a module', () => {
    expect(ComputePair).toBeDefined();
  });
});

