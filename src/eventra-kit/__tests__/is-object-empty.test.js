import { describe, it, expect } from 'vitest';
import * as IsObjectEmpty from '../is-object-empty.js';

describe('is-object-empty', () => {
  it('exports a module', () => {
    expect(IsObjectEmpty).toBeDefined();
  });
});

