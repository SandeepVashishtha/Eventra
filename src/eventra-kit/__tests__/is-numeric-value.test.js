import { describe, it, expect } from 'vitest';
import * as IsNumericValue from '../is-numeric-value.js';

describe('is-numeric-value', () => {
  it('exports a module', () => {
    expect(IsNumericValue).toBeDefined();
  });
});

