import { describe, it, expect } from 'vitest';
import * as CalculateHash from '../calculate-hash.js';

describe('calculate-hash', () => {
  it('exports a module', () => {
    expect(CalculateHash).toBeDefined();
  });
});

