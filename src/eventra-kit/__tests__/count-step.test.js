import { describe, it, expect } from 'vitest';
import * as CountStep from '../count-step.js';

describe('count-step', () => {
  it('exports a module', () => {
    expect(CountStep).toBeDefined();
  });
});

