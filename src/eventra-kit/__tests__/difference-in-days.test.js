import { describe, it, expect } from 'vitest';
import * as DifferenceInDays from '../difference-in-days.js';

describe('difference-in-days', () => {
  it('exports a module', () => {
    expect(DifferenceInDays).toBeDefined();
  });
});

