import { describe, it, expect } from 'vitest';
import * as DedupeSegment from '../dedupe-segment.js';

describe('dedupe-segment', () => {
  it('exports a module', () => {
    expect(DedupeSegment).toBeDefined();
  });
});

