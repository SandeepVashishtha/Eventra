import { describe, it, expect } from 'vitest';
import * as DistanceBetween from '../distance-between.js';

describe('distance-between', () => {
  it('exports a module', () => {
    expect(DistanceBetween).toBeDefined();
  });
});

