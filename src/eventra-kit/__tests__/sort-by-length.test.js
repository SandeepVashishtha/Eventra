import { describe, it, expect } from 'vitest';
import * as SortByLength from '../sort-by-length.js';

describe('sort-by-length', () => {
  it('exports a module', () => {
    expect(SortByLength).toBeDefined();
  });
});

