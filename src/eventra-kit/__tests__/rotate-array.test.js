import { describe, it, expect } from 'vitest';
import * as RotateArray from '../rotate-array.js';

describe('rotate-array', () => {
  it('exports a module', () => {
    expect(RotateArray).toBeDefined();
  });
});

