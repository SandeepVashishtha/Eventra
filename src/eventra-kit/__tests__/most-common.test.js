import { describe, it, expect } from 'vitest';
import * as MostCommon from '../most-common.js';

describe('most-common', () => {
  it('exports a module', () => {
    expect(MostCommon).toBeDefined();
  });
});

