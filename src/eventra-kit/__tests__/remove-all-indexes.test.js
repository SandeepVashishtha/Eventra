import { describe, it, expect } from 'vitest';
import * as RemoveAllIndexes from '../remove-all-indexes.js';

describe('remove-all-indexes', () => {
  it('exports a module', () => {
    expect(RemoveAllIndexes).toBeDefined();
  });
});

