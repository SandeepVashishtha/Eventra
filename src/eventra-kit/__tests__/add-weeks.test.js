import { describe, it, expect } from 'vitest';
import * as AddWeeks from '../add-weeks.js';

describe('add-weeks', () => {
  it('exports a module', () => {
    expect(AddWeeks).toBeDefined();
  });
});

