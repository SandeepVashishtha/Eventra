import { describe, it, expect } from 'vitest';
import * as LineCountOf from '../line-count-of.js';

describe('line-count-of', () => {
  it('exports a module', () => {
    expect(LineCountOf).toBeDefined();
  });
});

