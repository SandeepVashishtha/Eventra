import { describe, it, expect } from 'vitest';
import * as BubbleSort from '../bubble-sort.js';

describe('bubble-sort', () => {
  it('exports a module', () => {
    expect(BubbleSort).toBeDefined();
  });
});

