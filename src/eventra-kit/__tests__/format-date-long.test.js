import { describe, it, expect } from 'vitest';
import * as FormatDateLong from '../format-date-long.js';

describe('format-date-long', () => {
  it('exports a module', () => {
    expect(FormatDateLong).toBeDefined();
  });
});

