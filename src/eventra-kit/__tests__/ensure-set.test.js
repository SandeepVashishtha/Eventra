import { describe, it, expect } from 'vitest';
import * as EnsureSet from '../ensure-set.js';

describe('ensure-set', () => {
  it('exports a module', () => {
    expect(EnsureSet).toBeDefined();
  });
});

