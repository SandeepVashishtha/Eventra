import { describe, it, expect } from 'vitest';
import * as DiffArray from '../diff-array.js';

describe('diff-array', () => {
  it('exports a module', () => {
    expect(DiffArray).toBeDefined();
  });
});

