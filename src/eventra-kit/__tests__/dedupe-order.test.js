import { describe, it, expect } from 'vitest';
import * as DedupeOrder from '../dedupe-order.js';

describe('dedupe-order', () => {
  it('exports a module', () => {
    expect(DedupeOrder).toBeDefined();
  });
});

