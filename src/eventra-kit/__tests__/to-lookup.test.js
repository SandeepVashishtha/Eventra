import { describe, it, expect } from 'vitest';
import * as ToLookup from '../to-lookup.js';

describe('to-lookup', () => {
  it('exports a module', () => {
    expect(ToLookup).toBeDefined();
  });
});

