import { describe, it, expect } from 'vitest';
import * as CountSlice from '../count-slice.js';

describe('count-slice', () => {
  it('exports a module', () => {
    expect(CountSlice).toBeDefined();
  });
});

