import { describe, it, expect } from 'vitest';
import * as IsFloatValue from '../is-float-value.js';

describe('is-float-value', () => {
  it('exports a module', () => {
    expect(IsFloatValue).toBeDefined();
  });
});

