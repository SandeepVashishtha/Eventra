import { describe, it, expect } from 'vitest';
import * as AssertWeight from '../assert-weight.js';

describe('assert-weight', () => {
  it('exports a module', () => {
    expect(AssertWeight).toBeDefined();
  });
});

