import { describe, it, expect } from 'vitest';
import * as DetectGraph from '../detect-graph.js';

describe('detect-graph', () => {
  it('exports a module', () => {
    expect(DetectGraph).toBeDefined();
  });
});

