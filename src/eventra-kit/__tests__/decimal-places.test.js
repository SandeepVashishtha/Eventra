import { describe, it, expect } from 'vitest';
import * as DecimalPlaces from '../decimal-places.js';

describe('decimal-places', () => {
  it('exports a module', () => {
    expect(DecimalPlaces).toBeDefined();
  });
});

