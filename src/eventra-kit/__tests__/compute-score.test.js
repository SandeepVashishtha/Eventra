import { describe, it, expect } from 'vitest';
import * as ComputeScore from '../compute-score.js';

describe('compute-score', () => {
  it('exports a module', () => {
    expect(ComputeScore).toBeDefined();
  });
});

