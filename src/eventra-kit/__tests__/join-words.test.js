import { describe, it, expect } from 'vitest';
import * as JoinWords from '../join-words.js';

describe('join-words', () => {
  it('exports a module', () => {
    expect(JoinWords).toBeDefined();
  });
});

