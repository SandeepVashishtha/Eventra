import { describe, it, expect } from 'vitest';
import * as DedupeProp from '../dedupe-prop.js';

describe('dedupe-prop', () => {
  it('exports a module', () => {
    expect(DedupeProp).toBeDefined();
  });
});

