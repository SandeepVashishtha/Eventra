import { describe, it, expect } from 'vitest';
import * as MeanAbsolute from '../mean-absolute.js';

describe('mean-absolute', () => {
  it('exports a module', () => {
    expect(MeanAbsolute).toBeDefined();
  });
});

