import { describe, it, expect } from 'vitest';
import * as DayOfYear from '../day-of-year.js';

describe('day-of-year', () => {
  it('exports a module', () => {
    expect(DayOfYear).toBeDefined();
  });
});

