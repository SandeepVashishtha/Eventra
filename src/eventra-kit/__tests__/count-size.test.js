import { describe, it, expect } from 'vitest';
import * as CountSize from '../count-size.js';

describe('count-size', () => {
  it('exports a module', () => {
    expect(CountSize).toBeDefined();
  });
});

