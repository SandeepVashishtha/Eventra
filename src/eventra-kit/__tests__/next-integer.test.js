import { describe, it, expect } from 'vitest';
import * as NextInteger from '../next-integer.js';

describe('next-integer', () => {
  it('exports a module', () => {
    expect(NextInteger).toBeDefined();
  });
});

