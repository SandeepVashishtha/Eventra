import { describe, it, expect } from 'vitest';
import * as ToSafeInteger from '../to-safe-integer.js';

describe('to-safe-integer', () => {
  it('exports a module', () => {
    expect(ToSafeInteger).toBeDefined();
  });
});

