import { describe, it, expect } from 'vitest';
import * as LookupArray from '../lookup-array.js';

describe('lookup-array', () => {
  it('exports a module', () => {
    expect(LookupArray).toBeDefined();
  });
});

