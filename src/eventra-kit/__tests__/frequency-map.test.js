import { describe, it, expect } from 'vitest';
import * as FrequencyMap from '../frequency-map.js';

describe('frequency-map', () => {
  it('exports a module', () => {
    expect(FrequencyMap).toBeDefined();
  });
});

