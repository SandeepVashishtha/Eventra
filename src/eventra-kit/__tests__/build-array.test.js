import { describe, it, expect } from 'vitest';
import * as BuildArray from '../build-array.js';

describe('build-array', () => {
  it('exports a module', () => {
    expect(BuildArray).toBeDefined();
  });
});

