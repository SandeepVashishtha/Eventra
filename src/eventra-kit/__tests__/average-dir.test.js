import { describe, it, expect } from 'vitest';
import * as AverageDir from '../average-dir.js';

describe('average-dir', () => {
  it('exports a module', () => {
    expect(AverageDir).toBeDefined();
  });
});

