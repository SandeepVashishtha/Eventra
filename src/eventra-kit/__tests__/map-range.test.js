import { describe, it, expect } from 'vitest';
import * as MapRange from '../map-range.js';

describe('map-range', () => {
  it('exports a module', () => {
    expect(MapRange).toBeDefined();
  });
});

