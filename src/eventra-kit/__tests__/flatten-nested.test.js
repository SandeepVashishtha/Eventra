import { describe, it, expect } from 'vitest';
import * as FlattenNested from '../flatten-nested.js';

describe('flatten-nested', () => {
  it('exports a module', () => {
    expect(FlattenNested).toBeDefined();
  });
});

