import { describe, it, expect } from 'vitest';
import * as NormalizePercent from '../normalize-percent.js';

describe('normalize-percent', () => {
  it('exports a module', () => {
    expect(NormalizePercent).toBeDefined();
  });
});

