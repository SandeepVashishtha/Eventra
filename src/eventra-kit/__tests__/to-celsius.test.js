import { describe, it, expect } from 'vitest';
import * as ToCelsius from '../to-celsius.js';

describe('to-celsius', () => {
  it('exports a module', () => {
    expect(ToCelsius).toBeDefined();
  });
});

