import { describe, it, expect } from 'vitest';
import * as EstimateLeaf from '../estimate-leaf.js';

describe('estimate-leaf', () => {
  it('exports a module', () => {
    expect(EstimateLeaf).toBeDefined();
  });
});

