import { describe, it, expect } from 'vitest';
import * as DetectKey from '../detect-key.js';

describe('detect-key', () => {
  it('exports a module', () => {
    expect(DetectKey).toBeDefined();
  });
});

