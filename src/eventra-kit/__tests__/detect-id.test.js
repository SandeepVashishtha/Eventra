import { describe, it, expect } from 'vitest';
import * as DetectId from '../detect-id.js';

describe('detect-id', () => {
  it('exports a module', () => {
    expect(DetectId).toBeDefined();
  });
});

