import { describe, it, expect } from 'vitest';
import * as Interleave from '../interleave.js';

describe('interleave', () => {
  it('exports a module', () => {
    expect(Interleave).toBeDefined();
  });
});

