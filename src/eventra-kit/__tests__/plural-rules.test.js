import { describe, it, expect } from 'vitest';
import * as PluralRules from '../plural-rules.js';

describe('plural-rules', () => {
  it('exports a module', () => {
    expect(PluralRules).toBeDefined();
  });
});

