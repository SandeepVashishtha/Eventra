import { describe, it, expect } from 'vitest';
import * as PercentDifference from '../percent-difference.js';

describe('percent-difference', () => {
  it('exports a module', () => {
    expect(PercentDifference).toBeDefined();
  });
});

