import { describe, it, expect } from 'vitest';
import * as IsPrimeNumber from '../is-prime-number.js';

describe('is-prime-number', () => {
  it('exports a module', () => {
    expect(IsPrimeNumber).toBeDefined();
  });
});

