import { describe, it, expect } from 'vitest';
import * as EstimateValue from '../estimate-value.js';

describe('estimate-value', () => {
  it('exports a module', () => {
    expect(EstimateValue).toBeDefined();
  });
});

