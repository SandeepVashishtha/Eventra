import { describe, it, expect } from 'vitest';
import * as TrimmedMean from '../trimmed-mean.js';

describe('trimmed-mean', () => {
  it('exports a module', () => {
    expect(TrimmedMean).toBeDefined();
  });
});

