import { describe, it, expect } from 'vitest';
import * as DetectEdge from '../detect-edge.js';

describe('detect-edge', () => {
  it('exports a module', () => {
    expect(DetectEdge).toBeDefined();
  });
});

