import { describe, it, expect } from 'vitest';
import * as IsMobileDevice from '../is-mobile-device.js';

describe('is-mobile-device', () => {
  it('exports a module', () => {
    expect(IsMobileDevice).toBeDefined();
  });
});

