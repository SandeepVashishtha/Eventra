import { describe, it, expect } from 'vitest';
import * as EnsureToken from '../ensure-token.js';

describe('ensure-token', () => {
  it('exports a module', () => {
    expect(EnsureToken).toBeDefined();
  });
});

