import { describe, it, expect } from 'vitest';
import * as PercentOfTotal from '../percent-of-total.js';

describe('percent-of-total', () => {
  it('exports a module', () => {
    expect(PercentOfTotal).toBeDefined();
  });
});

