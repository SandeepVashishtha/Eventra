import { describe, it, expect } from 'vitest';
import * as CheckRank from '../check-rank.js';

describe('check-rank', () => {
  it('exports a module', () => {
    expect(CheckRank).toBeDefined();
  });
});

