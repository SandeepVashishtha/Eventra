import { describe, it, expect } from 'vitest';
import * as ConvertElement from '../convert-element.js';

describe('convert-element', () => {
  it('exports a module', () => {
    expect(ConvertElement).toBeDefined();
  });
});

