import { describe, it, expect } from 'vitest';
import * as DetectNumber from '../detect-number.js';

describe('detect-number', () => {
  it('exports a module', () => {
    expect(DetectNumber).toBeDefined();
  });
});

