import { describe, it, expect } from 'vitest';
import * as EnsureField from '../ensure-field.js';

describe('ensure-field', () => {
  it('exports a module', () => {
    expect(EnsureField).toBeDefined();
  });
});

