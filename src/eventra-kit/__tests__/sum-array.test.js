import { describe, it, expect } from 'vitest';
import * as SumArray from '../sum-array.js';

describe('sum-array', () => {
  it('exports a module', () => {
    expect(SumArray).toBeDefined();
  });
});

