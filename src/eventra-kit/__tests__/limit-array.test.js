import { describe, it, expect } from 'vitest';
import * as LimitArray from '../limit-array.js';

describe('limit-array', () => {
  it('exports a module', () => {
    expect(LimitArray).toBeDefined();
  });
});

