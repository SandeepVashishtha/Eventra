import { describe, it, expect } from 'vitest';
import * as DiffPortion from '../diff-portion.js';

describe('diff-portion', () => {
  it('exports a module', () => {
    expect(DiffPortion).toBeDefined();
  });
});

