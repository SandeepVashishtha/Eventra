import { describe, it, expect } from 'vitest';
import * as DetectPage from '../detect-page.js';

describe('detect-page', () => {
  it('exports a module', () => {
    expect(DetectPage).toBeDefined();
  });
});

