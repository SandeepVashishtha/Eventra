import { describe, it, expect } from 'vitest';
import * as DiffCircle from '../diff-circle.js';

describe('diff-circle', () => {
  it('exports a module', () => {
    expect(DiffCircle).toBeDefined();
  });
});

