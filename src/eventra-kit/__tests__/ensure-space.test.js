import { describe, it, expect } from 'vitest';
import * as EnsureSpace from '../ensure-space.js';

describe('ensure-space', () => {
  it('exports a module', () => {
    expect(EnsureSpace).toBeDefined();
  });
});

