import { describe, it, expect } from 'vitest';
import * as PreventDefault from '../prevent-default.js';

describe('prevent-default', () => {
  it('exports a module', () => {
    expect(PreventDefault).toBeDefined();
  });
});

