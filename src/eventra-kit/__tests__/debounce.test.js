import { describe, it, expect } from 'vitest';
import * as Debounce from '../debounce.js';

describe('debounce', () => {
  it('exports a module', () => {
    expect(Debounce).toBeDefined();
  });
});

