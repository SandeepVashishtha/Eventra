import { describe, it, expect } from 'vitest';
import * as IsSnakeCase from '../is-snake-case.js';

describe('is-snake-case', () => {
  it('exports a module', () => {
    expect(IsSnakeCase).toBeDefined();
  });
});

