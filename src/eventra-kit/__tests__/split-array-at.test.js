import { describe, it, expect } from 'vitest';
import * as SplitArrayAt from '../split-array-at.js';

describe('split-array-at', () => {
  it('exports a module', () => {
    expect(SplitArrayAt).toBeDefined();
  });
});

