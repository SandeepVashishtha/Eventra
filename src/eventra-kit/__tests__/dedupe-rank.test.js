import { describe, it, expect } from 'vitest';
import * as DedupeRank from '../dedupe-rank.js';

describe('dedupe-rank', () => {
  it('exports a module', () => {
    expect(DedupeRank).toBeDefined();
  });
});

