import { describe, it, expect } from 'vitest';
import * as CountStack from '../count-stack.js';

describe('count-stack', () => {
  it('exports a module', () => {
    expect(CountStack).toBeDefined();
  });
});

