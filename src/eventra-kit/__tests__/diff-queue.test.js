import { describe, it, expect } from 'vitest';
import * as DiffQueue from '../diff-queue.js';

describe('diff-queue', () => {
  it('exports a module', () => {
    expect(DiffQueue).toBeDefined();
  });
});

