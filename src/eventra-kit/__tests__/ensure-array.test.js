import { describe, it, expect } from 'vitest';
import * as EnsureArray from '../ensure-array.js';

describe('ensure-array', () => {
  it('exports a module', () => {
    expect(EnsureArray).toBeDefined();
  });
});

