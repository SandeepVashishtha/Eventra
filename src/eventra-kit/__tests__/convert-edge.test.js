import { describe, it, expect } from 'vitest';
import * as ConvertEdge from '../convert-edge.js';

describe('convert-edge', () => {
  it('exports a module', () => {
    expect(ConvertEdge).toBeDefined();
  });
});

