import { describe, it, expect } from 'vitest';
import * as CircularShift from '../circular-shift.js';

describe('circular-shift', () => {
  it('exports a module', () => {
    expect(CircularShift).toBeDefined();
  });
});

