import { describe, it, expect } from 'vitest';
import * as SortNumbers from '../sort-numbers.js';

describe('sort-numbers', () => {
  it('exports a module', () => {
    expect(SortNumbers).toBeDefined();
  });
});

