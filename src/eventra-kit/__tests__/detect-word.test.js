import { describe, it, expect } from 'vitest';
import * as DetectWord from '../detect-word.js';

describe('detect-word', () => {
  it('exports a module', () => {
    expect(DetectWord).toBeDefined();
  });
});

