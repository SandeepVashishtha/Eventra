import { describe, it, expect } from 'vitest';
import * as ToNumber from '../to-number.js';

describe('to-number', () => {
  it('exports a module', () => {
    expect(ToNumber).toBeDefined();
  });
});

