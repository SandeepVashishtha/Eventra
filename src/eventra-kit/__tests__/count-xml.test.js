import { describe, it, expect } from 'vitest';
import * as CountXml from '../count-xml.js';

describe('count-xml', () => {
  it('exports a module', () => {
    expect(CountXml).toBeDefined();
  });
});

