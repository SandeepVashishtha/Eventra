import { describe, it, expect } from 'vitest';
import * as DetectGroup from '../detect-group.js';

describe('detect-group', () => {
  it('exports a module', () => {
    expect(DetectGroup).toBeDefined();
  });
});

