import { describe, it, expect } from 'vitest';
import * as DetectTree from '../detect-tree.js';

describe('detect-tree', () => {
  it('exports a module', () => {
    expect(DetectTree).toBeDefined();
  });
});

