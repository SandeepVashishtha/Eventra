import { describe, it, expect } from 'vitest';
import * as DiffScore from '../diff-score.js';

describe('diff-score', () => {
  it('exports a module', () => {
    expect(DiffScore).toBeDefined();
  });
});

