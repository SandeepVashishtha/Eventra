import { describe, it, expect } from 'vitest';
import * as ToBoolean from '../to-boolean.js';

describe('to-boolean', () => {
  it('exports a module', () => {
    expect(ToBoolean).toBeDefined();
  });
});

