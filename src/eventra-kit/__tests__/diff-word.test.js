import { describe, it, expect } from 'vitest';
import * as DiffWord from '../diff-word.js';

describe('diff-word', () => {
  it('exports a module', () => {
    expect(DiffWord).toBeDefined();
  });
});

