import { describe, it, expect } from 'vitest';
import * as LeastCommonChar from '../least-common-char.js';

describe('least-common-char', () => {
  it('exports a module', () => {
    expect(LeastCommonChar).toBeDefined();
  });
});

