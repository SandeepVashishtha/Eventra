import { describe, it, expect } from 'vitest';
import * as DetectToken from '../detect-token.js';

describe('detect-token', () => {
  it('exports a module', () => {
    expect(DetectToken).toBeDefined();
  });
});

