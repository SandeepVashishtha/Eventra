import { describe, it, expect } from 'vitest';
import * as DetectOrder from '../detect-order.js';

describe('detect-order', () => {
  it('exports a module', () => {
    expect(DetectOrder).toBeDefined();
  });
});

