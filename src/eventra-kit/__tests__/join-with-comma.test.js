import { describe, it, expect } from 'vitest';
import * as JoinWithComma from '../join-with-comma.js';

describe('join-with-comma', () => {
  it('exports a module', () => {
    expect(JoinWithComma).toBeDefined();
  });
});

