import { describe, it, expect } from 'vitest';
import * as MoveItem from '../move-item.js';

describe('move-item', () => {
  it('exports a module', () => {
    expect(MoveItem).toBeDefined();
  });
});

