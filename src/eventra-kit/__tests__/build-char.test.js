import { describe, it, expect } from 'vitest';
import * as BuildChar from '../build-char.js';

describe('build-char', () => {
  it('exports a module', () => {
    expect(BuildChar).toBeDefined();
  });
});

