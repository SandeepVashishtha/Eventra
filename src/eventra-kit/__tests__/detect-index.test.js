import { describe, it, expect } from 'vitest';
import * as DetectIndex from '../detect-index.js';

describe('detect-index', () => {
  it('exports a module', () => {
    expect(DetectIndex).toBeDefined();
  });
});

