import { describe, it, expect } from 'vitest';
import * as ClampName from '../clamp-name.js';

describe('clamp-name', () => {
  it('exports a module', () => {
    expect(ClampName).toBeDefined();
  });
});

