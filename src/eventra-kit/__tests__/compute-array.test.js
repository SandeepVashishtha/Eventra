import { describe, it, expect } from 'vitest';
import * as ComputeArray from '../compute-array.js';

describe('compute-array', () => {
  it('exports a module', () => {
    expect(ComputeArray).toBeDefined();
  });
});

