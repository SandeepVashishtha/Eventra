import { describe, it, expect } from 'vitest';
import * as IsDarkColor from '../is-dark-color.js';

describe('is-dark-color', () => {
  it('exports a module', () => {
    expect(IsDarkColor).toBeDefined();
  });
});

