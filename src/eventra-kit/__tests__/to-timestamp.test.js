import { describe, it, expect } from 'vitest';
import * as ToTimestamp from '../to-timestamp.js';

describe('to-timestamp', () => {
  it('exports a module', () => {
    expect(ToTimestamp).toBeDefined();
  });
});

