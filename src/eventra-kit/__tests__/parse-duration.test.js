import { describe, it, expect } from 'vitest';
import * as ParseDuration from '../parse-duration.js';

describe('parse-duration', () => {
  it('exports a module', () => {
    expect(ParseDuration).toBeDefined();
  });
});

