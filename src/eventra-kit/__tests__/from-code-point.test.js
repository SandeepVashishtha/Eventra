import { describe, it, expect } from 'vitest';
import * as FromCodePoint from '../from-code-point.js';

describe('from-code-point', () => {
  it('exports a module', () => {
    expect(FromCodePoint).toBeDefined();
  });
});

