import { describe, it, expect } from 'vitest';
import * as DiffObject from '../diff-object.js';

describe('diff-object', () => {
  it('exports a module', () => {
    expect(DiffObject).toBeDefined();
  });
});

