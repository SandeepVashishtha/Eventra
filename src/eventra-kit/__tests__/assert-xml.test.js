import { describe, it, expect } from 'vitest';
import * as AssertXml from '../assert-xml.js';

describe('assert-xml', () => {
  it('exports a module', () => {
    expect(AssertXml).toBeDefined();
  });
});

