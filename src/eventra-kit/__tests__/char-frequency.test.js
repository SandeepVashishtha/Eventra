import { describe, it, expect } from 'vitest';
import * as CharFrequency from '../char-frequency.js';

describe('char-frequency', () => {
  it('exports a module', () => {
    expect(CharFrequency).toBeDefined();
  });
});

