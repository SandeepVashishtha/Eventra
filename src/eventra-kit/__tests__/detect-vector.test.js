import { describe, it, expect } from 'vitest';
import * as DetectVector from '../detect-vector.js';

describe('detect-vector', () => {
  it('exports a module', () => {
    expect(DetectVector).toBeDefined();
  });
});

