import { describe, it, expect } from 'vitest';
import * as CheckByte from '../check-byte.js';

describe('check-byte', () => {
  it('exports a module', () => {
    expect(CheckByte).toBeDefined();
  });
});

