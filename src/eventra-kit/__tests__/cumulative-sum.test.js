import { describe, it, expect } from 'vitest';
import * as CumulativeSum from '../cumulative-sum.js';

describe('cumulative-sum', () => {
  it('exports a module', () => {
    expect(CumulativeSum).toBeDefined();
  });
});

