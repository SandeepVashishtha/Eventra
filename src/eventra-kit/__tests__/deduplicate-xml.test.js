import { describe, it, expect } from 'vitest';
import * as DeduplicateXml from '../deduplicate-xml.js';

describe('deduplicate-xml', () => {
  it('exports a module', () => {
    expect(DeduplicateXml).toBeDefined();
  });
});

