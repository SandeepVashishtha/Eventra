import { describe, it, expect } from 'vitest';
import * as ComputeChunk from '../compute-chunk.js';

describe('compute-chunk', () => {
  it('exports a module', () => {
    expect(ComputeChunk).toBeDefined();
  });
});

