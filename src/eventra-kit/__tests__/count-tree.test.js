import { describe, it, expect } from 'vitest';
import * as CountTree from '../count-tree.js';

describe('count-tree', () => {
  it('exports a module', () => {
    expect(CountTree).toBeDefined();
  });
});

