import { describe, it, expect } from 'vitest';
import * as GetQueryParam from '../get-query-param.js';

describe('get-query-param', () => {
  it('exports a module', () => {
    expect(GetQueryParam).toBeDefined();
  });
});

