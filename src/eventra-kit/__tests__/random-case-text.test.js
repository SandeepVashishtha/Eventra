import { describe, it, expect } from 'vitest';
import * as RandomCaseText from '../random-case-text.js';

describe('random-case-text', () => {
  it('exports a module', () => {
    expect(RandomCaseText).toBeDefined();
  });
});

