import { describe, it, expect } from 'vitest';
import * as GreedyChunks from '../greedy-chunks.js';

describe('greedy-chunks', () => {
  it('exports a module', () => {
    expect(GreedyChunks).toBeDefined();
  });
});

