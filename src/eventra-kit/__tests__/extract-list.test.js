import { describe, it, expect } from 'vitest';
import * as ExtractList from '../extract-list.js';

describe('extract-list', () => {
  it('exports a module', () => {
    expect(ExtractList).toBeDefined();
  });
});

