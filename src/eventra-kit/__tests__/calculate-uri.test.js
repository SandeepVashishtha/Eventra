import { describe, it, expect } from 'vitest';
import * as CalculateUri from '../calculate-uri.js';

describe('calculate-uri', () => {
  it('exports a module', () => {
    expect(CalculateUri).toBeDefined();
  });
});

