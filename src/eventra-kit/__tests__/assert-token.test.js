import { describe, it, expect } from 'vitest';
import * as AssertToken from '../assert-token.js';

describe('assert-token', () => {
  it('exports a module', () => {
    expect(AssertToken).toBeDefined();
  });
});

