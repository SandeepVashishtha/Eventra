import { describe, it, expect } from 'vitest';
import * as ScrollLock from '../scroll-lock.js';

describe('scroll-lock', () => {
  it('exports a module', () => {
    expect(ScrollLock).toBeDefined();
  });
});

