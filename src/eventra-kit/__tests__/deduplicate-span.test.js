import { describe, it, expect } from 'vitest';
import * as DeduplicateSpan from '../deduplicate-span.js';

describe('deduplicate-span', () => {
  it('exports a module', () => {
    expect(DeduplicateSpan).toBeDefined();
  });
});

