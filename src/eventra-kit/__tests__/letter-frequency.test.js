import { describe, it, expect } from 'vitest';
import * as LetterFrequency from '../letter-frequency.js';

describe('letter-frequency', () => {
  it('exports a module', () => {
    expect(LetterFrequency).toBeDefined();
  });
});

