import { describe, it, expect } from 'vitest';
import * as IsElementVisible from '../is-element-visible.js';

describe('is-element-visible', () => {
  it('exports a module', () => {
    expect(IsElementVisible).toBeDefined();
  });
});

