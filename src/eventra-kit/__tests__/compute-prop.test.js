import { describe, it, expect } from 'vitest';
import * as ComputeProp from '../compute-prop.js';

describe('compute-prop', () => {
  it('exports a module', () => {
    expect(ComputeProp).toBeDefined();
  });
});

