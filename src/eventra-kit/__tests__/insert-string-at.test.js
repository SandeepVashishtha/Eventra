import { describe, it, expect } from 'vitest';
import * as InsertStringAt from '../insert-string-at.js';

describe('insert-string-at', () => {
  it('exports a module', () => {
    expect(InsertStringAt).toBeDefined();
  });
});

