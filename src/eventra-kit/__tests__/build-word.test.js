import { describe, it, expect } from 'vitest';
import * as BuildWord from '../build-word.js';

describe('build-word', () => {
  it('exports a module', () => {
    expect(BuildWord).toBeDefined();
  });
});

