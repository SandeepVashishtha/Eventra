import { describe, it, expect } from 'vitest';
import * as GetUniqueValues from '../get-unique-values.js';

describe('get-unique-values', () => {
  it('exports a module', () => {
    expect(GetUniqueValues).toBeDefined();
  });
});

