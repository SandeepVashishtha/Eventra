import { describe, it, expect } from 'vitest';
import * as StartOfDay from '../start-of-day.js';

describe('start-of-day', () => {
  it('exports a module', () => {
    expect(StartOfDay).toBeDefined();
  });
});

