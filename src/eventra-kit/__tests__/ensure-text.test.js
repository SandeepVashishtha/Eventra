import { describe, it, expect } from 'vitest';
import * as EnsureText from '../ensure-text.js';

describe('ensure-text', () => {
  it('exports a module', () => {
    expect(EnsureText).toBeDefined();
  });
});

