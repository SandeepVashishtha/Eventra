import { describe, it, expect } from 'vitest';
import * as ComputeBox from '../compute-box.js';

describe('compute-box', () => {
  it('exports a module', () => {
    expect(ComputeBox).toBeDefined();
  });
});

