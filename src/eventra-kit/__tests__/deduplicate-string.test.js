import { describe, it, expect } from 'vitest';
import * as DeduplicateString from '../deduplicate-string.js';

describe('deduplicate-string', () => {
  it('exports a module', () => {
    expect(DeduplicateString).toBeDefined();
  });
});

