import { describe, it, expect } from 'vitest';
import * as StandardizeArray from '../standardize-array.js';

describe('standardize-array', () => {
  it('exports a module', () => {
    expect(StandardizeArray).toBeDefined();
  });
});

