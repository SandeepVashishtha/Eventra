import { describe, it, expect } from 'vitest';
import * as CheckCircle from '../check-circle.js';

describe('check-circle', () => {
  it('exports a module', () => {
    expect(CheckCircle).toBeDefined();
  });
});

