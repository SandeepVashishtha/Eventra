import { describe, it, expect } from 'vitest';
import * as DeduplicateIndex from '../deduplicate-index.js';

describe('deduplicate-index', () => {
  it('exports a module', () => {
    expect(DeduplicateIndex).toBeDefined();
  });
});

