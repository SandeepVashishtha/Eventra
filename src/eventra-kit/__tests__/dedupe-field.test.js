import { describe, it, expect } from 'vitest';
import * as DedupeField from '../dedupe-field.js';

describe('dedupe-field', () => {
  it('exports a module', () => {
    expect(DedupeField).toBeDefined();
  });
});

