import { describe, it, expect } from 'vitest';
import * as EnsureCircle from '../ensure-circle.js';

describe('ensure-circle', () => {
  it('exports a module', () => {
    expect(EnsureCircle).toBeDefined();
  });
});

