import { describe, it, expect } from 'vitest';
import * as TakeLast from '../take-last.js';

describe('take-last', () => {
  it('exports a module', () => {
    expect(TakeLast).toBeDefined();
  });
});

