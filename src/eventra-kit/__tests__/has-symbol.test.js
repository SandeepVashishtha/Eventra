import { describe, it, expect } from 'vitest';
import * as HasSymbol from '../has-symbol.js';

describe('has-symbol', () => {
  it('exports a module', () => {
    expect(HasSymbol).toBeDefined();
  });
});

