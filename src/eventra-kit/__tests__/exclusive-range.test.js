import { describe, it, expect } from 'vitest';
import * as ExclusiveRange from '../exclusive-range.js';

describe('exclusive-range', () => {
  it('exports a module', () => {
    expect(ExclusiveRange).toBeDefined();
  });
});

