import { describe, it, expect } from 'vitest';
import * as WordFrequency from '../word-frequency.js';

describe('word-frequency', () => {
  it('exports a module', () => {
    expect(WordFrequency).toBeDefined();
  });
});

