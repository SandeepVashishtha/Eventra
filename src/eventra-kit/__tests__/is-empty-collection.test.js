import { describe, it, expect } from 'vitest';
import * as IsEmptyCollection from '../is-empty-collection.js';

describe('is-empty-collection', () => {
  it('exports a module', () => {
    expect(IsEmptyCollection).toBeDefined();
  });
});

