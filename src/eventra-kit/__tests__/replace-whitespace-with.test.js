import { describe, it, expect } from 'vitest';
import * as ReplaceWhitespaceWith from '../replace-whitespace-with.js';

describe('replace-whitespace-with', () => {
  it('exports a module', () => {
    expect(ReplaceWhitespaceWith).toBeDefined();
  });
});

