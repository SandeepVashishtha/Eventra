import { describe, it, expect } from 'vitest';
import * as ConvertBox from '../convert-box.js';

describe('convert-box', () => {
  it('exports a module', () => {
    expect(ConvertBox).toBeDefined();
  });
});

