import { describe, it, expect } from 'vitest';
import * as ToTitleSlug from '../to-title-slug.js';

describe('to-title-slug', () => {
  it('exports a module', () => {
    expect(ToTitleSlug).toBeDefined();
  });
});

