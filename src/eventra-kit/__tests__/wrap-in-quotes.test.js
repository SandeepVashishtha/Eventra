import { describe, it, expect } from 'vitest';
import * as WrapInQuotes from '../wrap-in-quotes.js';

describe('wrap-in-quotes', () => {
  it('exports a module', () => {
    expect(WrapInQuotes).toBeDefined();
  });
});

