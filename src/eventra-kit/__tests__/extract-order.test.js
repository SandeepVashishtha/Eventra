import { describe, it, expect } from 'vitest';
import * as ExtractOrder from '../extract-order.js';

describe('extract-order', () => {
  it('exports a module', () => {
    expect(ExtractOrder).toBeDefined();
  });
});

