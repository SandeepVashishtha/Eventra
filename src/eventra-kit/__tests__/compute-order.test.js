import { describe, it, expect } from 'vitest';
import * as ComputeOrder from '../compute-order.js';

describe('compute-order', () => {
  it('exports a module', () => {
    expect(ComputeOrder).toBeDefined();
  });
});

