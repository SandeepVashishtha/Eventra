import { describe, it, expect } from 'vitest';
import * as EnsureChar from '../ensure-char.js';

describe('ensure-char', () => {
  it('exports a module', () => {
    expect(EnsureChar).toBeDefined();
  });
});

