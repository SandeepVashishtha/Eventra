import { describe, it, expect } from 'vitest';
import * as CompareStrings from '../compare-strings.js';

describe('compare-strings', () => {
  it('exports a module', () => {
    expect(CompareStrings).toBeDefined();
  });
});

