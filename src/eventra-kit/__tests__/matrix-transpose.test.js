import { describe, it, expect } from 'vitest';
import * as MatrixTranspose from '../matrix-transpose.js';

describe('matrix-transpose', () => {
  it('exports a module', () => {
    expect(MatrixTranspose).toBeDefined();
  });
});

