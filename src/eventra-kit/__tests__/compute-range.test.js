import { describe, it, expect } from 'vitest';
import * as ComputeRange from '../compute-range.js';

describe('compute-range', () => {
  it('exports a module', () => {
    expect(ComputeRange).toBeDefined();
  });
});

