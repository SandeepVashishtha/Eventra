import { describe, it, expect } from 'vitest';
import * as DiffRange from '../diff-range.js';

describe('diff-range', () => {
  it('exports a module', () => {
    expect(DiffRange).toBeDefined();
  });
});

