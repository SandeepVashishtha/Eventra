import { describe, it, expect } from 'vitest';
import * as AverageXml from '../average-xml.js';

describe('average-xml', () => {
  it('exports a module', () => {
    expect(AverageXml).toBeDefined();
  });
});

