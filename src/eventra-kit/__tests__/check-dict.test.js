import { describe, it, expect } from 'vitest';
import * as CheckDict from '../check-dict.js';

describe('check-dict', () => {
  it('exports a module', () => {
    expect(CheckDict).toBeDefined();
  });
});

