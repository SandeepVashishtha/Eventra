import { describe, it, expect } from 'vitest';
import * as CapitalizeFirstOfEach from '../capitalize-first-of-each.js';

describe('capitalize-first-of-each', () => {
  it('exports a module', () => {
    expect(CapitalizeFirstOfEach).toBeDefined();
  });
});

