import { describe, it, expect } from 'vitest';
import * as DetectMap from '../detect-map.js';

describe('detect-map', () => {
  it('exports a module', () => {
    expect(DetectMap).toBeDefined();
  });
});

