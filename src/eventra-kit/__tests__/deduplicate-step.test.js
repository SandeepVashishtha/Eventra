import { describe, it, expect } from 'vitest';
import * as DeduplicateStep from '../deduplicate-step.js';

describe('deduplicate-step', () => {
  it('exports a module', () => {
    expect(DeduplicateStep).toBeDefined();
  });
});

