import { describe, it, expect } from 'vitest';
import * as RelativeTime from '../relative-time.js';

describe('relative-time', () => {
  it('exports a module', () => {
    expect(RelativeTime).toBeDefined();
  });
});

