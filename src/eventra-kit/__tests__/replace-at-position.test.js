import { describe, it, expect } from 'vitest';
import * as ReplaceAtPosition from '../replace-at-position.js';

describe('replace-at-position', () => {
  it('exports a module', () => {
    expect(ReplaceAtPosition).toBeDefined();
  });
});

