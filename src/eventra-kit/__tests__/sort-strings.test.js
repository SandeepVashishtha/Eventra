import { describe, it, expect } from 'vitest';
import * as SortStrings from '../sort-strings.js';

describe('sort-strings', () => {
  it('exports a module', () => {
    expect(SortStrings).toBeDefined();
  });
});

