import { describe, it, expect } from 'vitest';
import * as StringToColor from '../string-to-color.js';

describe('string-to-color', () => {
  it('exports a module', () => {
    expect(StringToColor).toBeDefined();
  });
});

