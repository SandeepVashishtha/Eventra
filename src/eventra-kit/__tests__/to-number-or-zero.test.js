import { describe, it, expect } from 'vitest';
import * as ToNumberOrZero from '../to-number-or-zero.js';

describe('to-number-or-zero', () => {
  it('exports a module', () => {
    expect(ToNumberOrZero).toBeDefined();
  });
});

