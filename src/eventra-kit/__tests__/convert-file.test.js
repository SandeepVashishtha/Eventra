import { describe, it, expect } from 'vitest';
import * as ConvertFile from '../convert-file.js';

describe('convert-file', () => {
  it('exports a module', () => {
    expect(ConvertFile).toBeDefined();
  });
});

