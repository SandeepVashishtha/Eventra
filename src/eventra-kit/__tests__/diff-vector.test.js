import { describe, it, expect } from 'vitest';
import * as DiffVector from '../diff-vector.js';

describe('diff-vector', () => {
  it('exports a module', () => {
    expect(DiffVector).toBeDefined();
  });
});

