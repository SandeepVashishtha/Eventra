import { describe, it, expect } from 'vitest';
import * as SentenceFrequency from '../sentence-frequency.js';

describe('sentence-frequency', () => {
  it('exports a module', () => {
    expect(SentenceFrequency).toBeDefined();
  });
});

