import { describe, it, expect } from 'vitest';
import * as FormatWords from '../format-words.js';

describe('format-words', () => {
  it('exports a module', () => {
    expect(FormatWords).toBeDefined();
  });
});

