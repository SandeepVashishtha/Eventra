import { describe, it, expect } from 'vitest';
import * as ClassifyNumber from '../classify-number.js';

describe('classify-number', () => {
  it('exports a module', () => {
    expect(ClassifyNumber).toBeDefined();
  });
});

