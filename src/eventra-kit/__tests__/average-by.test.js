import { describe, it, expect } from 'vitest';
import * as AverageBy from '../average-by.js';

describe('average-by', () => {
  it('exports a module', () => {
    expect(AverageBy).toBeDefined();
  });
});

