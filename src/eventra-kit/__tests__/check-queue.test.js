import { describe, it, expect } from 'vitest';
import * as CheckQueue from '../check-queue.js';

describe('check-queue', () => {
  it('exports a module', () => {
    expect(CheckQueue).toBeDefined();
  });
});

