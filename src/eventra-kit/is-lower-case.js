
/**
 * adds a case check.
 */
export function isLowerCase(str) {
  return String(str) === String(str).toLowerCase();
}

export function isUpperCase(str) {
  return String(str) === String(str).toUpperCase();
}

