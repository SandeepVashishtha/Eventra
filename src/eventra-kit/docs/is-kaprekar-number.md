# isKaprekarNumber

Computes or checks values using the `isKaprekarNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isKaprekarNumber } from "../is-kaprekar-number.js";

console.log(isKaprekarNumber(5));
```
