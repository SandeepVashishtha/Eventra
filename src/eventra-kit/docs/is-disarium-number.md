# isDisariumNumber

Computes or checks values using the `isDisariumNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isDisariumNumber } from "../is-disarium-number.js";

console.log(isDisariumNumber(5));
```
