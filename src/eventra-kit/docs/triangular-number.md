# triangularNumber

Computes or checks values using the `triangularNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { triangularNumber } from "../triangular-number.js";

console.log(triangularNumber(5));
```
