# isPellNumber

Computes or checks values using the `isPellNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isPellNumber } from "../is-pell-number.js";

console.log(isPellNumber(5));
```
