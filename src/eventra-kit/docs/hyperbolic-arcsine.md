# hyperbolicArcsine

Computes or checks values using the `hyperbolicArcsine` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { hyperbolicArcsine } from "../hyperbolic-arcsine.js";

console.log(hyperbolicArcsine(5));
```
