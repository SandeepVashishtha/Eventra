# pellNumber

Computes or checks values using the `pellNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { pellNumber } from "../pell-number.js";

console.log(pellNumber(5));
```
