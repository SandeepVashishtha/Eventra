# isArmstrongNumber

Computes or checks values using the `isArmstrongNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isArmstrongNumber } from "../is-armstrong-number.js";

console.log(isArmstrongNumber(5));
```
