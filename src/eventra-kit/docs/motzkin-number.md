# motzkinNumber

Computes or checks values using the `motzkinNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { motzkinNumber } from "../motzkin-number.js";

console.log(motzkinNumber(5));
```
