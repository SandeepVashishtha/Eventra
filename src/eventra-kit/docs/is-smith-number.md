# isSmithNumber

Computes or checks values using the `isSmithNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isSmithNumber } from "../is-smith-number.js";

console.log(isSmithNumber(5));
```
