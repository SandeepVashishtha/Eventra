# isHexagonalNumber

Computes or checks values using the `isHexagonalNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isHexagonalNumber } from "../is-hexagonal-number.js";

console.log(isHexagonalNumber(5));
```
