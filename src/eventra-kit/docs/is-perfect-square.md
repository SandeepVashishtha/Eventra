# isPerfectSquare

Computes or checks values using the `isPerfectSquare` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isPerfectSquare } from "../is-perfect-square.js";

console.log(isPerfectSquare(5));
```
