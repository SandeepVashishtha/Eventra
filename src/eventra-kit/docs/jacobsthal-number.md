# jacobsthalNumber

Computes or checks values using the `jacobsthalNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { jacobsthalNumber } from "../jacobsthal-number.js";

console.log(jacobsthalNumber(5));
```
