# isPowerOfTen

Computes or checks values using the `isPowerOfTen` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isPowerOfTen } from "../is-power-of-ten.js";

console.log(isPowerOfTen(5));
```
