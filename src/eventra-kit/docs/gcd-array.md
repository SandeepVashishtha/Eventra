# gcdArray

Computes or checks values using the `gcdArray` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { gcdArray } from "../gcd-array.js";

console.log(gcdArray(5));
```
