# isPowerOfFour

Computes or checks values using the `isPowerOfFour` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isPowerOfFour } from "../is-power-of-four.js";

console.log(isPowerOfFour(5));
```
