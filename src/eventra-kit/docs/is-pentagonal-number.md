# isPentagonalNumber

Computes or checks values using the `isPentagonalNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isPentagonalNumber } from "../is-pentagonal-number.js";

console.log(isPentagonalNumber(5));
```
