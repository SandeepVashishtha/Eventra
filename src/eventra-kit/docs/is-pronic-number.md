# isPronicNumber

Computes or checks values using the `isPronicNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isPronicNumber } from "../is-pronic-number.js";

console.log(isPronicNumber(5));
```
