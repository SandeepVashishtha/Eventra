# isPerfectNumber

Computes or checks values using the `isPerfectNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isPerfectNumber } from "../is-perfect-number.js";

console.log(isPerfectNumber(5));
```
