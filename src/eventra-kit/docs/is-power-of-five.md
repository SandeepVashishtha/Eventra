# isPowerOfFive

Computes or checks values using the `isPowerOfFive` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isPowerOfFive } from "../is-power-of-five.js";

console.log(isPowerOfFive(5));
```
