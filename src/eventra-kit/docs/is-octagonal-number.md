# isOctagonalNumber

Computes or checks values using the `isOctagonalNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isOctagonalNumber } from "../is-octagonal-number.js";

console.log(isOctagonalNumber(5));
```
