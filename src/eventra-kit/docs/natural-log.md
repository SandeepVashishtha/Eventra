# naturalLog

Computes or checks values using the `naturalLog` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { naturalLog } from "../natural-log.js";

console.log(naturalLog(5));
```
