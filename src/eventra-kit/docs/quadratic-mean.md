# quadraticMean

Computes or checks values using the `quadraticMean` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { quadraticMean } from "../quadratic-mean.js";

console.log(quadraticMean(5));
```
