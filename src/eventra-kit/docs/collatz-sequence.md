# collatzSequence

Computes or checks values using the `collatzSequence` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { collatzSequence } from "../collatz-sequence.js";

console.log(collatzSequence(5));
```
