# turnsToGradients

Computes or checks values using the `turnsToGradients` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { turnsToGradients } from "../turns-to-gradients.js";

console.log(turnsToGradients(5));
```
