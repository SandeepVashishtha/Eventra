# doubleFactorial

Computes or checks values using the `doubleFactorial` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { doubleFactorial } from "../double-factorial.js";

console.log(doubleFactorial(5));
```
