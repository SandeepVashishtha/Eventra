# gradientsToTurns

Computes or checks values using the `gradientsToTurns` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { gradientsToTurns } from "../gradients-to-turns.js";

console.log(gradientsToTurns(5));
```
