# lcmArray

Computes or checks values using the `lcmArray` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { lcmArray } from "../lcm-array.js";

console.log(lcmArray(5));
```
