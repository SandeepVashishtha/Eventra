# averageDeviation

Computes or checks values using the `averageDeviation` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { averageDeviation } from "../average-deviation.js";

console.log(averageDeviation(5));
```
