# logBase2

Computes or checks values using the `logBase2` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { logBase2 } from "../log-base-2.js";

console.log(logBase2(5));
```
