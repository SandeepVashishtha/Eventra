# isPandigitalNumber

Computes or checks values using the `isPandigitalNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isPandigitalNumber } from "../is-pandigital-number.js";

console.log(isPandigitalNumber(5));
```
