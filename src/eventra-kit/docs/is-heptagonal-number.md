# isHeptagonalNumber

Computes or checks values using the `isHeptagonalNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isHeptagonalNumber } from "../is-heptagonal-number.js";

console.log(isHeptagonalNumber(5));
```
