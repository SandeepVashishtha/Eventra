# gradientsToDegrees

Computes or checks values using the `gradientsToDegrees` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { gradientsToDegrees } from "../gradients-to-degrees.js";

console.log(gradientsToDegrees(5));
```
