# radiansToGradients

Computes or checks values using the `radiansToGradients` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { radiansToGradients } from "../radians-to-gradients.js";

console.log(radiansToGradients(5));
```
