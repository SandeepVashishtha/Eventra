# harmonicMean

Computes or checks values using the `harmonicMean` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { harmonicMean } from "../harmonic-mean.js";

console.log(harmonicMean([1, 2, 4]));
```
