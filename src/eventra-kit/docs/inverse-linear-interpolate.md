# inverseLinearInterpolate

Computes or checks values using the `inverseLinearInterpolate` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { inverseLinearInterpolate } from "../inverse-linear-interpolate.js";

console.log(inverseLinearInterpolate(5));
```
