# degreesToMinutes

Computes or checks values using the `degreesToMinutes` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { degreesToMinutes } from "../degrees-to-minutes.js";

console.log(degreesToMinutes(5));
```
