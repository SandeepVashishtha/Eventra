# isTriangularNumber

Computes or checks values using the `isTriangularNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isTriangularNumber } from "../is-triangular-number.js";

console.log(isTriangularNumber(5));
```
