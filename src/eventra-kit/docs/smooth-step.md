# smoothStep

Computes or checks values using the `smoothStep` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { smoothStep } from "../smooth-step.js";

console.log(smoothStep(5));
```
