# lerpArray

Computes or checks values using the `lerpArray` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { lerpArray } from "../lerp-array.js";

console.log(lerpArray(5));
```
