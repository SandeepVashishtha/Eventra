# isPowerOfThree

Computes or checks values using the `isPowerOfThree` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isPowerOfThree } from "../is-power-of-three.js";

console.log(isPowerOfThree(5));
```
