# catalanNumber

Computes or checks values using the `catalanNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { catalanNumber } from "../catalan-number.js";

console.log(catalanNumber(5));
```
