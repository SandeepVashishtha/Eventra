# isFibonacciNumber

Computes or checks values using the `isFibonacciNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isFibonacciNumber } from "../is-fibonacci-number.js";

console.log(isFibonacciNumber(5));
```
