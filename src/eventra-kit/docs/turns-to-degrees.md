# turnsToDegrees

Computes or checks values using the `turnsToDegrees` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { turnsToDegrees } from "../turns-to-degrees.js";

console.log(turnsToDegrees(5));
```
