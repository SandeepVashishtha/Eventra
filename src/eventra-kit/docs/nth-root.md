# nthRoot

Computes or checks values using the `nthRoot` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { nthRoot } from "../nth-root.js";

console.log(nthRoot(5));
```
