# isAbundantNumber

Computes or checks values using the `isAbundantNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isAbundantNumber } from "../is-abundant-number.js";

console.log(isAbundantNumber(5));
```
