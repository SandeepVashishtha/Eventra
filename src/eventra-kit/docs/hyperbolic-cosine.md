# hyperbolicCosine

Computes or checks values using the `hyperbolicCosine` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { hyperbolicCosine } from "../hyperbolic-cosine.js";

console.log(hyperbolicCosine(5));
```
