# isHarshadNumber

Computes or checks values using the `isHarshadNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isHarshadNumber } from "../is-harshad-number.js";

console.log(isHarshadNumber(5));
```
