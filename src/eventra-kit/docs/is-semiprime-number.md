# isSemiprimeNumber

Computes or checks values using the `isSemiprimeNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isSemiprimeNumber } from "../is-semiprime-number.js";

console.log(isSemiprimeNumber(5));
```
