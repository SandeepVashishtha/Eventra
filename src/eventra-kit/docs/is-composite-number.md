# isCompositeNumber

Computes or checks values using the `isCompositeNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isCompositeNumber } from "../is-composite-number.js";

console.log(isCompositeNumber(5));
```
