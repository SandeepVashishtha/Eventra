# standardError

Computes or checks values using the `standardError` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { standardError } from "../standard-error.js";

console.log(standardError(5));
```
