# degreesToTurns

Computes or checks values using the `degreesToTurns` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { degreesToTurns } from "../degrees-to-turns.js";

console.log(degreesToTurns(5));
```
