# degreesToGradients

Computes or checks values using the `degreesToGradients` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { degreesToGradients } from "../degrees-to-gradients.js";

console.log(degreesToGradients(5));
```
