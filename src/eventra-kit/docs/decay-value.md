# decayValue

Computes or checks values using the `decayValue` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { decayValue } from "../decay-value.js";

console.log(decayValue(5));
```
