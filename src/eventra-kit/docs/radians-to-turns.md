# radiansToTurns

Computes or checks values using the `radiansToTurns` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { radiansToTurns } from "../radians-to-turns.js";

console.log(radiansToTurns(5));
```
