# isLucasNumber

Computes or checks values using the `isLucasNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isLucasNumber } from "../is-lucas-number.js";

console.log(isLucasNumber(5));
```
