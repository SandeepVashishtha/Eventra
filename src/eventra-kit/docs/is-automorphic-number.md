# isAutomorphicNumber

Computes or checks values using the `isAutomorphicNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isAutomorphicNumber } from "../is-automorphic-number.js";

console.log(isAutomorphicNumber(5));
```
