# pentagonalNumber

Computes or checks values using the `pentagonalNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { pentagonalNumber } from "../pentagonal-number.js";

console.log(pentagonalNumber(5));
```
