# isTribonacciNumber

Computes or checks values using the `isTribonacciNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { isTribonacciNumber } from "../is-tribonacci-number.js";

console.log(isTribonacciNumber(5));
```
