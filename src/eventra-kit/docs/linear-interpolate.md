# linearInterpolate

Computes or checks values using the `linearInterpolate` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { linearInterpolate } from "../linear-interpolate.js";

console.log(linearInterpolate(5));
```
