# lucasNumber

Computes or checks values using the `lucasNumber` utility.

## Description

This helper is added to `eventra-kit` to support mathematical calculations.

## Usage

```js
import { lucasNumber } from "../lucas-number.js";

console.log(lucasNumber(5));
```
