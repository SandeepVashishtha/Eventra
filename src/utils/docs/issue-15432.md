# Issue #15432 Resolution

Resolved: fix(utils): resolve syntax error in compressor utilities

## Description
Removed stray closing braces inside the `simpleCompress` and `simpleDecompress` helper functions within `src/utils/compressor.js`, resolving build and syntax compilation issues.
