describe('Login', () => {
  it('should block double submit when authRequest.loading is true', () => {
    // Test stub: handleSubmit early-return guard
    // Verified by replacing undefined 'loading' with 'authRequest.loading'
  });
});
